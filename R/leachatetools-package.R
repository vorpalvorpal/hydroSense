#' @keywords internal
"_PACKAGE"

# All dplyr/tidyr/tibble calls across the package are namespace-qualified
# (dplyr::, tidyr::, tibble::), so no package namespace is imported here -- the
# packages only need to be listed under Imports in DESCRIPTION.

NULL
