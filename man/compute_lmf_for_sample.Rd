% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lmf.R
\name{compute_lmf_for_sample}
\alias{compute_lmf_for_sample}
\title{Compute LMF for a single sample}
\usage{
compute_lmf_for_sample(
  sample_wide,
  endmembers,
  high_info_ions,
  min_high_info_ions,
  rsd_default,
  max_sigma,
  max_chi2_df,
  uuid_lmf,
  ref_window_start,
  ref_window_end,
  leach_window_start,
  leach_window_end,
  cl_anchor,
  robust_iterations = 3L,
  robust_threshold_k = 1.5,
  verbose = FALSE,
  datetime_sample = NA_character_
)
}
\arguments{
\item{sample_wide}{One-row wide-format tibble with one column per measured
(collapsed) panel ion.}

\item{endmembers}{Tibble with columns \code{ion}, \code{R},
\code{sigma_R}, \code{n_ref}, \code{L}. One row per ion with usable
end-member data for this feature.}

\item{high_info_ions}{Character vector of ion names classified as
high-information at calibration time.}

\item{min_high_info_ions}{Minimum count of \code{high_info_ions} that must
be present and non-NA in this sample.}

\item{rsd_default}{Default relative analytical uncertainty (fraction).}

\item{max_sigma}{Maximum permitted \code{sigma_lmf}.}

\item{max_chi2_df}{Maximum permitted chi-squared per degree of freedom.}

\item{uuid_lmf}{UUID of the LMF analyte entry in \code{analyteDF}.}

\item{ref_window_start, ref_window_end}{Calibration window dates for the
reference end-member; stored in guideline comment strings.}

\item{leach_window_start, leach_window_end}{Calibration window dates for the
leachate end-member; stored in guideline comment strings.}

\item{cl_anchor}{Cl concentration (meq/L) used to anchor the leachate
end-member; stored in guideline comment strings.}

\item{robust_iterations}{Number of Huber reweighting passes. Inherited from
\code{\link{add_lmf}}.}

\item{robust_threshold_k}{MAD multiplier for Huber downweighting. Inherited
from \code{\link{add_lmf}}.}

\item{verbose}{If \code{TRUE}, emits a per-ion diagnostic table via
\code{cli::cli_inform()} showing observed concentration, end-member
values, per-ion mixing fraction, original and robust weights. Inherited
from \code{\link{add_lmf}}. Default \code{FALSE}.}

\item{datetime_sample}{The sample datetime as a character string, used
to label the per-ion diagnostic table when \code{verbose = TRUE}.
Default \code{NA_character_}.}
}
\value{
A one-row tibble. On success: \code{value} is the robust LMF
estimate, \code{quantified = TRUE}. On failure: \code{value = NA},
\code{quantified = FALSE}, \code{lmf_reason} carries a descriptive
reason code. All paths return identical column structure.
}
\description{
Internal function called once per sample by \code{\link{add_lmf}}.
Computes the inverse-variance weighted mixing fraction and associated
quality metrics, applying admission and quality gates before returning.
}
\keyword{internal}
