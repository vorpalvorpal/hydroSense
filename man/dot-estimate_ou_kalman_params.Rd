% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kalman_bridge.R
\name{.estimate_ou_kalman_params}
\alias{.estimate_ou_kalman_params}
\title{Estimate OU parameters (gamma by MoM, theta by MLE) with a fallback ladder}
\usage{
.estimate_ou_kalman_params(anchor_dates, anchor_S, n_fit_min = 8L, scale = 1)
}
\arguments{
\item{anchor_dates}{Date (or coercible) vector of anchor dates.}

\item{anchor_S}{Numeric residual S at each anchor (same length).}

\item{n_fit_min}{Minimum anchors to attempt the theta MLE (default 8).}

\item{scale}{Positive multiplier on \code{gamma} (the envelope-width knob;
correlation length / theta is unchanged). Default 1.}
}
\value{
list(theta, gamma, tier).
}
\description{
\code{gamma} (the marginal/stationary variance, the well-identified moment) is the
sample variance of the anchor residuals, times \code{scale}. \code{theta} (the
mean-reversion rate / inverse correlation length, weakly identified on sparse
data) is fitted by 1-D MLE of the exact irregular-spacing OU likelihood,
bounded so the correlation length stays in \link{0.5x, 10x} the median anchor
spacing.
}
\details{
Tiers: \code{"degenerate"} (< 2 finite anchors or ~constant S -> no uncertainty),
\code{"fallback"} (>= 2 but < \code{n_fit_min} anchors -> theta pinned at the long-
correlation bound, approximating a Brownian bridge without diffuse init),
\code{"mle"} (>= \code{n_fit_min} -> theta by MLE).
}
\keyword{internal}
