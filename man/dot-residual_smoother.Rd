% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kalman_bridge.R
\name{.residual_smoother}
\alias{.residual_smoother}
\title{Fit + smooth the residual for one analyte over a (clipped) daily grid}
\usage{
.residual_smoother(
  anchor_dates,
  anchor_S,
  target_dates,
  z_hydro = NULL,
  kappa = 0.5,
  n_fit_min = 8L,
  scale = 1,
  r_vec = NULL
)
}
\arguments{
\item{anchor_dates, anchor_S}{Observed anchors.}

\item{target_dates}{Candidate daily grid (will be clipped to the grab span).}

\item{z_hydro}{Optional standardised hydro feature aligned to \code{target_dates}
(same length); drives \code{q_mult = exp(kappa * z)}. \code{NULL} -> no modulation.}

\item{kappa}{Hydrology process-variance sensitivity (default 0.5).}

\item{n_fit_min, scale}{Passed to \code{\link[=.estimate_ou_kalman_params]{.estimate_ou_kalman_params()}}.}

\item{r_vec}{Per-anchor observation variance (default tiny).}
}
\value{
list(grid_dates, params, model, mean, var). \code{model} is \code{NULL} for the
degenerate path.
}
\description{
Clips \code{target_dates} to the analyte's grab span \verb{[first_anchor, last_anchor]}
(no extrapolation beyond the grabs), fits the OU parameters, builds the KFAS
model with optional hydrology-modulated process variance, and returns the
smoothed posterior. For degenerate series returns a flat mean with zero
variance.
}
\keyword{internal}
