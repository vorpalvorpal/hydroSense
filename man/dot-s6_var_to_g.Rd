% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform.R
\name{.s6_var_to_g}
\alias{.s6_var_to_g}
\title{Map an observation variance onto the g scale (delta method)}
\usage{
.s6_var_to_g(var_x, x, scale_c)
}
\arguments{
\item{var_x}{Observation variance in concentration/impact space (numeric).}

\item{x}{The level at which the error is evaluated (impact \code{I} for the impact
tier, concentration for the WQ tier).}

\item{scale_c}{Single positive transform scale \code{c}.}
}
\value{
The observation variance on the \code{g} scale, \code{var_x / (x^2 + c^2)}.
}
\description{
The grab measurement error (S6) is specified as a variance in
concentration/impact space; the residual smoother now works on the
\code{g = asinh(x / c)} scale (issue #15), so the observation noise must be
transformed. By the delta method \verb{Var(g) = Var(x) * g'(x)^2}, and with
\verb{g'(x) = 1 / sqrt(x^2 + c^2)} this is \code{Var(x) / (x^2 + c^2)}. With
multiplicative grab error \code{Var(x) = (cv*x)^2} this plateaus at \code{cv^2} for
\verb{|x| >> c} (proportional error) and vanishes at baseline.
}
\keyword{internal}
