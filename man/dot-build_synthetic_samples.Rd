% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amspaf_daily.R
\name{.build_synthetic_samples}
\alias{.build_synthetic_samples}
\title{Build synthetic long-format daily samples from interpolated chemistry}
\usage{
.build_synthetic_samples(daily_long, site)
}
\arguments{
\item{daily_long}{Output of \code{.build_daily_chem()} (after temperature fill).}

\item{site}{Site identifier string.}
}
\value{
Long-format tibble ready for \code{\link[=add_amspaf]{add_amspaf()}} (after removing \code{.date}).
}
\description{
Assigns \code{sample_id = "daily_{YYYY-MM-DD}_{site}"} per day.  Keeps \code{.date}
as a column (the caller extracts it before passing to \code{\link[=add_amspaf]{add_amspaf()}}).
No \code{focal_date} column is added -- this is deliberate so \code{\link[=add_amspaf]{add_amspaf()}}
uses the instant (pointwise) ARA path, not the chronic integrated path.
}
\keyword{internal}
