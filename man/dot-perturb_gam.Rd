% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draws.R
\name{.perturb_gam}
\alias{.perturb_gam}
\title{Perturb a single mgcv GAM by drawing from its posterior coefficient distribution}
\usage{
.perturb_gam(g)
}
\arguments{
\item{g}{A fitted \code{mgcv::gam} object, or \code{NULL}.}
}
\value{
\code{g} with \verb{$coefficients} replaced by a single posterior draw,
or \code{g} unchanged on failure.
}
\description{
Draws one set of coefficients from \code{N(coef(g), Vp)} and assigns them to
\code{g$coefficients} so that subsequent \code{predict.gam()} calls use the draw.
\code{predict.gam()} dispatches through \code{g$coefficients}, so this gives an
honest posterior-predictive draw without refitting.
}
\details{
Returns \code{g} unperturbed when \verb{$Vp} is absent, NULL, or when the draw
produces non-finite values (near-singular posterior).  A symmetric PD
jitter is attempted once before giving up.
}
\keyword{internal}
