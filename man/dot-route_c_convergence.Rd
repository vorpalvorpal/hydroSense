% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/route-c.R
\name{.route_c_convergence}
\alias{.route_c_convergence}
\title{Rotation-invariant convergence diagnostics for a Route C factor fit}
\usage{
.route_c_convergence(group)
}
\arguments{
\item{group}{A fitted \code{factor}-method group (from \code{\link[=.fit_group_model_factor]{.fit_group_model_factor()}}).}
}
\value{
A list with \code{sigma_rhat}, \code{psi_rhat}, \code{lp_rhat} (max Rhat over the
\code{Sigma} entries, over \code{psi}, and for \code{lp__}); all \code{NA} for a degenerate
(single-analyte, Stan-free) group.
}
\description{
A low-rank factor model is only identified up to rotation of \code{Lambda}
(\verb{Lambda R} gives the same \verb{Lambda Lambda'}). With the positive-lower-
triangular constraint this is mostly pinned, but at small \code{N} / weak signal
the sampler can still explore near-rotationally-equivalent modes, inflating
the per-element Rhat of \code{Lambda} \strong{without any consequence for imputation} --
prediction uses only \verb{Sigma = Lambda Lambda' + Psi}, which is rotation-
invariant (see \code{\link[=.factor_condition]{.factor_condition()}}). So the meaningful convergence check
monitors the invariant functionals: the implied residual covariance \code{Sigma},
the idiosyncratic variances \code{psi}, and the log-density \code{lp__} -- not raw
\code{Lambda}. Empirically \code{Sigma} converges cleanly (Rhat ~ 1.0) even when a
\code{Lambda} element trips a naive \code{max(rhat(fit))} gate.
}
\keyword{internal}
