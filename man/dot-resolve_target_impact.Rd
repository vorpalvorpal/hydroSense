% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/target_model.R
\name{.resolve_target_impact}
\alias{.resolve_target_impact}
\title{Predict the site impact (and implied normalised concentration) at query dates}
\usage{
.resolve_target_impact(target_model, query, analytes = NULL)
}
\arguments{
\item{target_model}{A \code{target_model}.}

\item{query}{Tibble with \code{date} (Date) — the days to predict.}

\item{analytes}{Character; analytes to predict (default: all modelled).}
}
\value{
Tibble \verb{(date, analyte, ref_norm, impact, C_norm, impact_tier)}.
}
\description{
For each query (date × analyte): \verb{I_hat = beta·f(hydro) + S_interp}, with
\verb{beta·f(hydro)} from the fitted response (0 for bridge-tier analytes) and
\code{S_interp} from \code{\link[=.interp_residual]{.interp_residual()}}.  Also returns \code{ref_norm} (from the
reference model) and the implied \code{C_norm = max(ref_norm + I_hat, 0)}.
}
\keyword{internal}
