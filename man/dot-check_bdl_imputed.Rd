% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{.check_bdl_imputed}
\alias{.check_bdl_imputed}
\title{Post-hoc BDL check (Option B): warn/cap imputed values exceeding detection limit}
\usage{
.check_bdl_imputed(result, dl_tbl, cap = TRUE)
}
\description{
For BDL observations (\code{imputed_kind == "censored_left"}), if the imputed
posterior mean exceeds the original detection limit (stored in \code{dl_tbl}),
issue a warning. When \code{cap = TRUE}, the value is capped at the DL.
}
\details{
This check is a minimum safeguard against the loss of the strict left-censor
constraint caused by using \code{mi()} instead of \code{cens()} (the latter is
incompatible with \code{rescor = TRUE} in brms). See the \code{impute_chemistry()}
documentation for details on Option D (custom Stan), which would handle this
correctly during fitting.
}
\keyword{internal}
