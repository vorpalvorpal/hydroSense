% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hardness.R
\name{derive_hardness}
\alias{derive_hardness}
\title{Three-way reconciliation between Ca, Mg, and hardness}
\usage{
derive_hardness(df, tolerance = 0.05, verbose = TRUE)
}
\arguments{
\item{df}{Long-format chemistry data frame with columns
\code{sample_id}, \code{analyte}, \code{value}, \code{detected}.  Required columns
\code{site_id} and \code{datetime} are propagated to new rows from the existing
per-sample metadata.}

\item{tolerance}{Relative tolerance (proportion of measured hardness) for
consistency check when all three are present.  Default \code{0.05} (5\%).}

\item{verbose}{Logical.  If \code{TRUE}, prints a summary of how many rows were
derived and how many inconsistencies were detected.  Default \code{TRUE}.}
}
\value{
The input \code{df} with derived rows appended for \code{Ca}, \code{Mg}, or
\code{hardness} wherever exactly two of the three were available.  Derived
rows are tagged with \code{imputed = TRUE} and \code{imputed_kind = "derived"}
if those columns exist on the input (otherwise they are added).
}
\description{
Water hardness (total, expressed as CaCO3-equivalent mg/L) is stoichiometric
in Ca and Mg:
}
\details{
\deqn{hardness = 2.497 \cdot Ca + 4.118 \cdot Mg}

where Ca and Mg are in mg/L.  This helper fills in any missing member of
\{Ca, Mg, hardness\} when the other two are available, and warns if all
three are present but inconsistent.

\strong{Per-sample logic:}

\describe{
\item{All three present}{Check consistency.  If
\eqn{|\hat{hardness} - hardness| / hardness > tolerance}, emit a
per-sample warning and keep the user-supplied values.}
\item{Exactly two present}{Compute the third exactly from stoichiometry
and append as a new row with \code{detected = TRUE}.}
\item{One or none present}{Leave alone — fill via imputation if needed.}
}

\strong{Recommended pipeline use:}

Call twice — once \strong{before} imputation to fill samples where the third
member can be derived from raw measurements, and again \strong{after}
\code{impute_coanalytes()} to fill hardness for samples whose Ca and Mg were
just imputed.  Idempotent if all three are already consistent.
}
\examples{
\dontrun{
# Fill missing hardness where Ca and Mg are both measured
chem2 <- derive_hardness(chem)

# Call twice in a typical pipeline: pre- and post-imputation
chem      <- derive_hardness(chem)
chem_imp  <- impute_chemistry(chem, model)
chem_imp2 <- impute_coanalytes(chem_imp, model)
chem_imp3 <- derive_hardness(chem_imp2)
}

}
