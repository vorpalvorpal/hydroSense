% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/watertemp.R
\name{get_silo_air_temp}
\alias{get_silo_air_temp}
\title{Fetch daily mean air temperature from SILO for an Australian location}
\usage{
get_silo_air_temp(
  latitude,
  longitude,
  start_date,
  end_date,
  api_key = NULL,
  cache = TRUE,
  refresh = FALSE
)
}
\arguments{
\item{latitude, longitude}{Numeric decimal-degree coordinates of the point of
interest. Must fall within the SILO grid (approximately latitude -44 to
-10, longitude 112 to 154). Snapped to the 0.05° grid by SILO.}

\item{start_date, end_date}{Start and end of the (inclusive) date range, as
\code{Date} objects or \code{"YYYY-MM-DD"} strings.}

\item{api_key}{Character SILO API key (your email address). Default \code{NULL}
defers to \code{\link[weatherOz:get_key]{weatherOz::get_key()}}.}

\item{cache}{Logical; cache the result on disk under
\code{tools::R_user_dir("leachatetools", "cache")/silo}. Default \code{TRUE}.}

\item{refresh}{Logical; if \code{TRUE}, ignore and overwrite any cached result.
Default \code{FALSE}.}
}
\value{
A tibble with one row per day:
\itemize{
\item \code{datetime} (\code{Date})
\item \code{air_temp_mean_C} (numeric, °C)
Ready to pass as \code{air_temp_df} to \code{\link[=estimate_water_temp]{estimate_water_temp()}}.
}
}
\description{
Retrieves daily air temperature from the SILO Data Drill (a ~5 km gridded,
spatially interpolated climate surface covering Australia, 1889–present) for
a single latitude/longitude, and returns \strong{daily mean air temperature}
(\code{(Tmax + Tmin) / 2}, °C) in exactly the shape \code{\link[=estimate_water_temp]{estimate_water_temp()}}
expects as its \code{air_temp_df}. The typical workflow is:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{air   <- get_silo_air_temp(lat, lon, start, end)   # SILO mean air temp
wt    <- estimate_water_temp(air, water_temp_obs)  # calibrate water = f(air)
chem  <- dplyr::bind_rows(chem, dplyr::mutate(wt, sample_id = ...))
}\if{html}{\out{</div>}}

This wraps \code{\link[weatherOz:get_data_drill]{weatherOz::get_data_drill()}}; the \strong{weatherOz} package must be
installed (it is listed under \code{Suggests}). Results are cached on disk so
repeat calls for the same grid cell and date range do not re-hit the API.
}
\section{Attribution}{

SILO data are © State of Queensland (Department of Environment, Science and
Innovation) and released under CC-BY 4.0. Cite SILO when you publish results
derived from this function.
}

\section{API key}{

SILO requires an API key, which is simply your email address. By default it
is auto-detected via \code{\link[weatherOz:get_key]{weatherOz::get_key()}} (from \code{.Renviron}/\code{.Rprofile});
see the weatherOz documentation for one-time setup, or pass \code{api_key}
directly.
}

\examples{
\dontrun{
air <- get_silo_air_temp(
  latitude = -33.87, longitude = 151.21,
  start_date = "2020-01-01", end_date = "2023-12-31"
)
}
}
\seealso{
\code{\link[=estimate_water_temp]{estimate_water_temp()}}
}
