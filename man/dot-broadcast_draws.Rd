% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draws.R
\name{.broadcast_draws}
\alias{.broadcast_draws}
\title{Broadcast a draw-carrier frame to uniform draw coverage}
\usage{
.broadcast_draws(df, draws = .draw_domain(df))
}
\arguments{
\item{df}{Long-format data frame following the draw-carrier contract.}

\item{draws}{Integer vector of active draw IDs (default: \code{.draw_domain(df)}).}
}
\value{
\code{df} with \code{draw_id} filled for all rows; exact rows replicated N times.
}
\description{
After this call every row has a concrete integer \code{draw_id}; downstream code
can \code{group_by(draw_id)} with no \code{NA}-special-casing.
}
\details{
Exact cells (\code{draw_id = NA}) are replicated once per draw in \code{draws} with
\code{draw_id} filled.  Draw cells pass through unchanged, order preserved.

\strong{Call inside a per-site or per-sample scope} — not on the whole dataset —
because replication multiplies exact rows by N.
}
\keyword{internal}
