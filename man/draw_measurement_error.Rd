% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draws.R
\name{draw_measurement_error}
\alias{draw_measurement_error}
\title{Expand observed measurements into lognormal posterior-predictive draws}
\usage{
draw_measurement_error(
  df,
  error_col,
  error_type = c("cv", "sd"),
  ndraws = NULL,
  seed = NULL
)
}
\arguments{
\item{df}{Long-format data frame following the draw-carrier contract.}

\item{error_col}{Name (string) of the column carrying per-measurement
uncertainty.  Must exist in \code{df}.  Rows with NA or zero values are not
expanded.}

\item{error_type}{\code{"cv"} (default): \code{error_col} contains coefficients of
variation (e.g. \code{0.05} = 5 \% RSD, matching the \code{\link[=add_lmf]{add_lmf()}} convention).
\code{"sd"}: \code{error_col} contains absolute standard deviations on the natural
concentration scale; CV is derived as \code{sd / value}.}

\item{ndraws}{Number of draws.  Required when \code{df} carries no existing
draws.  When draws are already present N is inferred from the draw
domain; \code{ndraws} must match or be \code{NULL}.}

\item{seed}{Optional integer seed passed to \code{\link[=set.seed]{set.seed()}} before sampling,
for reproducibility.}
}
\value{
\code{df} with eligible observed cells replaced by N lognormal draw
rows (\verb{draw_id 1..N}).  Ineligible cells are unchanged.  Returns \code{df}
unmodified when no eligible cells exist (including when all \code{error_col}
values are \code{NA}).
}
\description{
Turns observed, detected, exact (\code{draw_id = NA}, \code{!imputed}) cells that
carry a per-measurement uncertainty column into N lognormal draws,
propagating analytical error through the pipeline alongside imputation
uncertainty from \code{\link[=impute_chemistry]{impute_chemistry()}} / \code{\link[=impute_coanalytes]{impute_coanalytes()}}.
}
\details{
Cells without reported error (NA or zero in \code{error_col}), BDL cells
(\code{detected = FALSE}), and already-drawn or imputed cells pass through
unchanged.

\strong{Lognormal parameterisation — geometric-mean convention:} the reported
value \eqn{v} is the geometric mean (median) of the draw distribution.
For coefficient of variation \eqn{CV}:

\deqn{\sigma_{\log} = \sqrt{\log(1 + CV^2)},\quad
      \mu = \log v,\quad
      \text{draws} = \exp(\mathcal{N}(\mu,\, \sigma_{\log}^2))}

The arithmetic mean of the draws is \eqn{v \exp(\sigma_{\log}^2/2)},
which exceeds \eqn{v} by < 0.5 \% at 10 \% RSD.

\strong{Documented independence approximations:} measurement error is treated
as independent of imputation error (cross-source index-pairing) and
independent across analytes within a sample.

\strong{Call order:} \code{impute_chemistry()} → \code{impute_coanalytes()} →
\code{draw_measurement_error()} → \code{add_amspaf()}.  Imputation establishes the
draw count N; this function reuses it.
}
\seealso{
\code{\link[=impute_chemistry]{impute_chemistry()}}, \code{\link[=impute_coanalytes]{impute_coanalytes()}}, \code{\link[=summarise_draws]{summarise_draws()}}
}
