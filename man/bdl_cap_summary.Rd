% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{bdl_cap_summary}
\alias{bdl_cap_summary}
\title{Inspect detection-limit cap activations from \code{impute_chemistry()}}
\usage{
bdl_cap_summary(x)
}
\arguments{
\item{x}{A data frame returned by \code{\link[=impute_chemistry]{impute_chemistry()}}.}
}
\value{
A tibble with one row per (\code{sample_id}, \code{analyte}) cell that exceeded
its detection limit, with columns \code{detection_limit}, \code{n_rows} (rows over
the DL — one per draw when \code{return = "draws"}), \code{max_imputed}, \code{max_ratio}
(\code{max_imputed / detection_limit}) and \code{capped} (whether the cap was
applied).  Returns \code{NULL} invisibly when no cell exceeded its DL.
}
\description{
An imputed below-detection (BDL) cell must not exceed its detection limit
(DL).  The posterior prediction is not itself constrained below the DL, so
\code{\link[=impute_chemistry]{impute_chemistry()}} caps any imputed BDL cell whose estimate came out above
the limit (\code{bdl_cap = TRUE}).  Frequent capping signals tension between the
modelled chemistry and the reported limits, so the cells that triggered the
cap are worth auditing rather than trusting blindly.
}
\details{
\code{impute_chemistry()} attaches a per-cell audit summary to its result as the
\code{"bdl_cap_summary"} attribute; this accessor returns it.  Because plain
attributes are dropped by most \pkg{dplyr} verbs, call this on the frame
\strong{as returned by \code{impute_chemistry()}}, before further wrangling.
}
\seealso{
\code{\link[=impute_chemistry]{impute_chemistry()}}
}
