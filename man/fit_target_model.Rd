% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/target_model.R
\name{fit_target_model}
\alias{fit_target_model}
\title{Fit a season-blind predictive model of the site impact}
\usage{
fit_target_model(
  target,
  reference_model,
  hydro = NULL,
  hydro_type = "rainfall",
  imputation_model = NULL,
  conc_units = NULL,
  analyte_metadata = NULL,
  method = c("multi", "anzecc"),
  guideline_dir = getOption("hydroSense.guideline_dir"),
  transform = c("pseudo_log", "additive"),
  analyte_c = NULL,
  api_tau_bounds_short = c(1, 30),
  api_tau_bounds_long = c(20, 365),
  auto_select = TRUE,
  min_obs_model = 12L,
  pool = TRUE,
  eps = 1e-09
)
}
\arguments{
\item{target}{Long-format target chemistry. Required columns: \code{sample_id},
\code{datetime}, \code{analyte}, \code{value}, \code{detected}. Toxicants must be in ug/L;
supply via a \code{units.analyte} column or \code{conc_units}. Co-analyte rows
(pH, DOC, hardness, temperature) should be present for normalisation.}

\item{reference_model}{A \code{reference_model} from \code{\link[=fit_reference_model]{fit_reference_model()}}.
Supplies \code{ref_norm(t)} and (by default) the catchment hydrology series.}

\item{hydro}{Optional target-specific daily hydrology data frame
(\code{date}, \code{value}); when \code{NULL} (default) the reference model's hydrology is
reused (shared-catchment assumption). A target-local stage/discharge gauge
can capture breach-mobilising flow that catchment rainfall misses.}

\item{hydro_type}{\code{"rainfall"}, \code{"stage"}, or \code{"discharge"}; used only when
\code{hydro} is supplied. Default \code{"rainfall"}.}

\item{imputation_model}{Optional \code{imputation_model} from
\code{\link[=fit_imputation_model]{fit_imputation_model()}} (fit on the target's chemistry). When supplied,
missing analytes are imputed in raw concentration space before the impact
is computed, adding more anchor days (tier 2). Requires \strong{brms}.}

\item{conc_units}{Unit string for target chemistry when no \code{units.analyte}
column is present.}

\item{analyte_metadata}{Analyte metadata, or \code{NULL} for the bundled CSV.}

\item{method}{SSD method (\code{"multi"} or \code{"anzecc"}) used to derive each
analyte's HC5 transform scale when \code{analyte_c} is not supplied.}

\item{guideline_dir}{Path to the ANZG guideline data folder (for the SSD
fits); falls back to \code{getOption("hydroSense.guideline_dir")}.}

\item{transform}{\code{"pseudo_log"} (default) or \code{"additive"}. Controls the
variance-stabilising transform applied to the impact residual before
smoothing. \code{"pseudo_log"} uses \code{g = asinh(I / c)} with per-analyte scale
\code{c = HC5} (issue #15), which compresses the dynamic range and prevents
event spikes from inflating the baseline draw spread. \code{"additive"} keeps
\code{g = I} (pre-#15 behaviour): the smoother operates in the original additive
impact space. Ignored when \code{analyte_c} is supplied directly.}

\item{analyte_c}{Optional named numeric vector of per-analyte transform
scales \code{c} (SSD HC5; issue #15). When \code{NULL} (default) it is computed from
the fitted SSDs. The impact residual is smoothed on the variance-stabilising
scale \code{g = asinh(I / c)}; an analyte with \code{NA}/absent \code{c} keeps the additive
model.}

\item{api_tau_bounds_short, api_tau_bounds_long}{Length-2 numeric \code{c(lo, hi)}
search ranges (days) for the short/long reservoir recession constants of
\code{f(hydro)}, selected per analyte by profiled AIC (with a
\code{tau_long >= 1.5*tau_short} separation).  A degenerate \code{c(x, x)} fixes that
store's tau.  Defaults \code{c(1, 30)} and \code{c(20, 365)}.}

\item{auto_select}{Logical; profiled-AIC tau selection per analyte (default
\code{TRUE}).  If \code{FALSE}, use the parsimonious defaults (tau 7 / 60 days).}

\item{min_obs_model}{Integer; minimum impact anchors required to attempt the
\code{f(hydro)} GAM. Below this, the analyte uses the bridge tier. Default \code{12L}.}

\item{pool}{Logical (default \code{TRUE}). When \code{TRUE}, the per-analyte hydro
responses are \strong{partially pooled}: a single factor-smooth GAM
(\code{bs = "fs"}) is fitted across all sufficiently-sampled analytes at one
common AIC-selected window, shrinking each analyte's response toward a
shared shape. This \emph{regularises} noisy, low-signal analytes (it does not
add hydrological coverage -- co-sampled analytes already share the same
regimes), and falls back to independent fits if it fails or doesn't beat an
analyte-intercept null. Set \code{pool = FALSE} to force independent per-analyte
fits (appropriate only when all analytes are densely sampled).}

\item{eps}{Small positive guard. Default \code{1e-9}.}
}
\value{
An object of class \code{target_model}:
\describe{
\item{\verb{$models}}{Named per-analyte list: \code{impact_fit} (gam or \code{NULL}),
\code{tau_short}, \code{tau_long}, \code{tier} (\code{"model"} or \code{"bridge"}),
\code{n_obs}, and \code{anchors} (tibble \code{date}, \code{I}, \code{S}, \code{hydro_short},
\code{hydro_long}).}
\item{\verb{$reference_model}}{The supplied reference model.}
\item{\verb{$hydro},\verb{$hydro_type}}{Hydrology series used for \code{f(hydro)}.}
\item{\verb{$fit_date}}{Date fitted.}
}
}
\description{
Models the anthropogenic increment \code{I = C_norm - ref_norm} (the ARA
"added risk", i.e. \code{ara_summary()}'s \code{C_excess}) at a target site as a
function of hydrology and a persistent latent state -- \strong{never} of
day-of-year.  Used by \code{\link[=amspaf_daily]{amspaf_daily()}} (\code{interpolation = "model"}) to fill
the gaps between grab samples with a chemistry-grounded impact estimate
instead of a forward-filled concentration.
}
\details{
\strong{Why season-blind.} Site impacts are driven by management failure (a
leachate breach), not the calendar.  Heavy summer rain \emph{enables} an existing
breach to escape; it does not \emph{cause} one.  A perfect-management year has
zero impact under identical rainfall.  Day-of-year is therefore a confounded
covariate for impact and is excluded; hydrology enters only as a modulator of
how an already-present impact expresses itself (first-flush mobilisation,
sorption, antecedent memory).

\strong{Model (per analyte).}
\deqn{I(t) = \beta \cdot f(\mathrm{hydro}_t) + S(t)}
where \code{f(hydro)} is a thin-plate GAM on the short- and long-window
antecedent indices (no cyclic seasonal term), fitted only when it beats an
intercept-only null by AIC and at least \code{min_obs_model} anchors are
available; otherwise \code{beta = 0} and the analyte falls to a pure
state-interpolation ("bridge") tier.  The residual state \code{S(t)} is
interpolated between observation anchors by a hydrology-weighted bridge that
pinches to the observed residual at each anchor and leans toward the
hydrologically more similar bracketing anchor in between.

This is a season-blind, hydrology-modulated variant of WRTDS-Kalman
(Zhang & Hirsch 2019); \code{f(hydro)} follows concentration--discharge theory
(Godsey, Kirchner & Clow 2009).
}
\examples{
\dontrun{
ref_model <- fit_reference_model(reference_chem,
  latitude = -33.8,
  longitude = 151.2, conc_units = "ug/L"
)
tgt_model <- fit_target_model(target_chem, ref_model, conc_units = "ug/L")
tgt_model
}
}
\references{
Zhang Q, Hirsch RM (2019) Water Resources Research 55(11):9705--9723.
Godsey SE, Kirchner JW, Clow DW (2009) Hydrological Processes 23:1844--1864.
}
\seealso{
\code{\link[=amspaf_daily]{amspaf_daily()}}, \code{\link[=fit_reference_model]{fit_reference_model()}}, \code{\link[=add_amspaf]{add_amspaf()}},
\code{\link[=ara_summary]{ara_summary()}}
}
