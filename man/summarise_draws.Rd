% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draws.R
\name{summarise_draws}
\alias{summarise_draws}
\title{Summarise a draw-carrier frame to posterior median and credible interval}
\usage{
summarise_draws(df, interval = 0.9, central = c("median", "mean"))
}
\arguments{
\item{df}{Long-format data frame following the draw-carrier contract.}

\item{interval}{Width of the credible interval.  Default \code{0.90} yields
5th / 95th percentile bounds.}

\item{central}{Central-tendency statistic: \code{"median"} (default) or
\code{"mean"}.}
}
\value{
\code{df} collapsed to one row per cell, with columns \code{value}
(central estimate), \code{value_lower}, \code{value_upper} (interval bounds), and
\code{n_draws}.  Returns \code{df} unchanged when the input carries no draws.
}
\description{
Collapses per-cell posterior draws to a point estimate and credible
interval.  Returns \code{df} unchanged (identity) when the input carries no
draws (point frame), preserving the degradation guarantee — no CI columns
appear for callers that never produce draws.
}
\details{
\strong{Per-draw diagnostic columns} (\code{draw_id}, \code{dominant_analyte}, \code{max_paf},
\code{analyte_pafs}) are dropped in draws mode; they are available by passing
\code{return = "draws"} to \code{\link[=add_mspaf]{add_mspaf()}} or \code{\link[=time_weighted_aggregate]{time_weighted_aggregate()}}.

Exact cells (\code{draw_id = NA}) each collapse to a degenerate interval:
\code{value_lower = value_upper = value}, \code{n_draws = 1}.
}
\examples{
\dontrun{
# Collapse msPAF draws to median + 90 \% CI
mspaf_draws |>
  add_mspaf(return = "draws") |>
  summarise_draws(interval = 0.90)
}
}
\seealso{
\code{\link[=add_mspaf]{add_mspaf()}}, \code{\link[=time_weighted_aggregate]{time_weighted_aggregate()}}
}
