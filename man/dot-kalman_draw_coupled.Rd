% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kalman_bridge.R
\name{.kalman_draw_coupled}
\alias{.kalman_draw_coupled}
\title{Draw correlated residual trajectories via the DK simulation smoother}
\usage{
.kalman_draw_coupled(setup, eta_std, a1_z, eps_std)
}
\arguments{
\item{setup}{Return value of \code{\link[=.kalman_sim_smoother_setup]{.kalman_sim_smoother_setup()}}.}

\item{eta_std}{\verb{[n_grid × nsim]} matrix of iid N(0,1) process innovations.}

\item{a1_z}{Length-\code{nsim} vector of N(0,1) initial-state noise.}

\item{eps_std}{\verb{[m × nsim]} matrix of iid N(0,1) observation noise
(one row per anchor position).}
}
\value{
Numeric matrix \verb{[n_grid × nsim]} of residual draws in original
(un-standardised) units.
}
\description{
Produces \code{nsim} draws from the Durbin–Koopman (2002) simulation smoother
using \strong{externally-supplied} standard-normal noise matrices. Because the
noise is passed in by the caller (rather than drawn internally), the caller
can correlate the innovations \emph{across analytes} while keeping every
per-analyte marginal distribution unchanged.
}
\details{
The DK identity (Algorithm 1 of Durbin & Koopman 2002):
\deqn{\tilde{\alpha} = \hat{\alpha} + (\alpha^+ - L y^+)}
where \eqn{\alpha^+} is a prior simulation driven by the supplied innovations
and \eqn{L y^+ = \hat{\alpha}^+} is the KFS smoother applied to the
synthetic observations \eqn{y^+}.

All arithmetic is performed in standardised units; the result is
un-standardised by \code{setup$resid_scale} before returning.
}
\references{
Durbin J, Koopman SJ (2002). A simple and efficient simulation
smoother for state space time series analysis. \emph{Biometrika} \strong{89}(3),
603–616. \url{https://doi.org/10.1093/biomet/89.3.603}
}
\keyword{internal}
