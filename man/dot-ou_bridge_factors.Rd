% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ou_bridge.R
\name{.ou_bridge_factors}
\alias{.ou_bridge_factors}
\title{Precompute Cholesky factors for OU bridge draws over a daily grid}
\usage{
.ou_bridge_factors(anchor_dates, target_dates, theta, sigma2, gamma)
}
\arguments{
\item{anchor_dates}{Date vector of anchor dates (need not be sorted; need
not all appear in \code{target_dates}).}

\item{target_dates}{Date vector of the full daily grid.}

\item{theta, sigma2, gamma}{OU parameters from \code{\link[=.estimate_ou_params]{.estimate_ou_params()}}.}
}
\value{
A list (the "factors" object) consumed by \code{\link[=.ou_bridge_draw]{.ou_bridge_draw()}}.
}
\description{
For each inter-anchor gap and for the leading/trailing extrapolation zones,
builds the conditional covariance of the latent residual state S given
zero values at the bracketing anchor dates, then Cholesky-factorises it.
}
\details{
Call this \strong{once} per (analyte) after fitting the target model.  Each of
the N per-draw calls to \code{\link[=.ou_bridge_draw]{.ou_bridge_draw()}} then requires only one
matrix-vector product per gap.

\strong{OU conditional covariance} for interior days \verb{\{t₁,…,tₘ\}} in gap \verb{[a,b]}
given \code{S(a) = S(b) = 0}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{Σ_cond[i,j] = γ·exp(-θ|tᵢ−tⱼ|) −
  γ·(uᵢuⱼ + vᵢvⱼ − s(uᵢvⱼ + vᵢuⱼ)) / (1−s²)
}\if{html}{\out{</div>}}

where \verb{uᵢ = exp(-θ(tᵢ−a))}, \verb{vᵢ = exp(-θ(b−tᵢ))}, \verb{s = exp(-θ(b−a))}.
As \verb{θ·(b−a) → 0} this equals the Brownian bridge covariance
\verb{σ²·min(tᵢ−a, tⱼ−a)·(b−max(tᵢ,tⱼ)) / (b−a)} — the code switches
automatically when \verb{θ·L < 1e-8}.
}
\keyword{internal}
