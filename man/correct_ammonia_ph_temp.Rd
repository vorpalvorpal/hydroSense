% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalise.R
\name{correct_ammonia_ph_temp}
\alias{correct_ammonia_ph_temp}
\title{Correct total ammonia-N to the ANZG reference pH and temperature}
\usage{
correct_ammonia_ph_temp(
  conc_ug_L,
  pH,
  temperature_C,
  ref_pH = 7,
  ref_temperature_C = 20
)
}
\arguments{
\item{conc_ug_L}{Measured total ammonia-N (µg/L). Numeric vector.}

\item{pH}{Sample pH. Recycled against \code{conc_ug_L}.}

\item{temperature_C}{Sample temperature (°C). Recycled against \code{conc_ug_L}.}

\item{ref_pH, ref_temperature_C}{Reference index condition. Defaults to the
ANZG ammonia DGV basis (pH 7.0, 20 °C); change only if your SSD/DGV uses a
different reference.}
}
\value{
Total ammonia-N normalised to the reference condition (µg/L), the
length of the recycled inputs. Pass this to \code{ssd_paf("NH3-N", ...)}.
}
\description{
The freshwater ammonia default guideline values (DGVs) and the bundled
ammonia SSD are expressed as \strong{total ammonia-N at the ANZG index condition of
pH 7.0 and 20 °C}. Ammonia toxicity is driven by the \emph{un-ionised} fraction
(NH3), which rises with both pH and temperature. A measured total ammonia-N
must therefore be converted to the equivalent reference-condition
concentration — the one that holds the same un-ionised NH3 — before it is
compared against a DGV or passed to \code{\link[=ssd_paf]{ssd_paf()}} as \code{"NH3-N"}.
}
\details{
The conversion is \code{C_ref = C * f_sample / f_ref}, where
\code{f = 1 / (1 + 10^(pKa - pH))} is the un-ionised fraction and
\code{pKa = 0.09018 + 2729.92 / T(K)} (Emerson et al. 1975), with
\code{T(K) = temperature_C + 273.15}. A sample at high pH/temperature is
normalised \strong{upward} (more of its ammonia is in the toxic NH3 form than at
the reference), and a sample at low pH/temperature \strong{downward}.
}
\section{Do not double-correct}{

\code{\link[=add_amspaf]{add_amspaf()}} applies this same correction \strong{automatically} from the
metadata \code{normalisation_formula} (it reads the per-sample \code{pH} and
\code{temperature} columns). Use this helper only for the manual \code{\link[=ssd_paf]{ssd_paf()}}
path. Do \strong{not} pre-correct with this helper and then pass the result to
\code{\link[=add_amspaf]{add_amspaf()}}, or ammonia will be corrected twice.
}

\examples{
# 900 µg/L total ammonia-N measured at pH 8.5, 20 °C is far more toxic than
# the same number at the pH 7.0 reference, so it normalises sharply upward:
correct_ammonia_ph_temp(900, pH = 8.5, temperature_C = 20)
}
\references{
Emerson K, Russo RC, Lund RE, Thurston RV (1975). Aqueous ammonia
equilibrium calculations: effect of pH and temperature. Journal of the
Fisheries Research Board of Canada 32(12):2379–2383.
}
