% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{impute_group}
\alias{impute_group}
\title{Declare an imputation group}
\usage{
impute_group(name, targets = NULL, hurdle = NULL)
}
\arguments{
\item{name}{Group label (a non-empty string).  Used as the name of the
group's slot in the fitted model's \verb{$groups} list and in console messages.}

\item{targets}{Character vector of analyte names to model jointly in this
group, or \code{NULL} to mark this as the \strong{catch-all} group, which claims
every remaining modellable analyte (those not excluded, not used as PCA
predictors, and not already claimed by an earlier group).  At most one
catch-all group is allowed in a \code{groups} list.}

\item{hurdle}{Character vector of analyte names defining a \emph{presence hurdle},
or \code{NULL} for no hurdle.  When a hurdle is set, a sample is only imputed
for this group if it carries at least one of these analytes (detected or
below-detection).  This stops silence being mistaken for absence — e.g. a
sample with no metals recorded is left alone rather than given invented
metal values.}
}
\value{
An object of class \code{"impute_group"}: a list with elements \code{name},
\code{targets}, and \code{hurdle}.
}
\description{
The imputation engine (\code{\link[=fit_imputation_model]{fit_imputation_model()}}) is domain-agnostic: it
imputes one or more \strong{groups} of target analytes from a shared
PCA-compressed chemistry context, with cross-target residual correlation
within each group.  \code{impute_group()} describes a single group — which
analytes it models and which (if any) presence hurdle gates it.  Pass a list
of these as the \code{groups} argument of \code{\link[=fit_imputation_model]{fit_imputation_model()}}.
}
\details{
Each group is fitted as its own joint \code{brms} model, so analytes in different
groups do not borrow residual correlation from one another.  Group together
analytes you expect to co-vary (e.g. metals that move together in a plume).
}
\examples{
# A metals group hurdled on metal presence, plus an everything-else group
# hurdled on dissolved-organic-carbon presence:
groups <- list(
  impute_group("metals", targets = c("Cu", "Zn", "Ni", "Pb"),
               hurdle = c("Cu", "Zn", "Ni", "Pb")),
  impute_group("organics", targets = NULL, hurdle = c("DOC", "TOC"))
)
}
\seealso{
\code{\link[=fit_imputation_model]{fit_imputation_model()}}, \code{\link[=leachate_impute_groups]{leachate_impute_groups()}}
}
