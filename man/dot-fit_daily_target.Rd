% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amspaf_daily.R
\name{.fit_daily_target}
\alias{.fit_daily_target}
\title{Fit-once scaffold for the season-blind daily target model (issue #16)}
\usage{
.fit_daily_target(
  site_rows,
  reference_model,
  imputation_model,
  conc_units,
  meta,
  tox_analytes,
  daily_long,
  ou_scale = 1,
  grab_cv = NULL,
  kappa = 0.5
)
}
\arguments{
\item{site_rows}{Grab chemistry for the site.}

\item{reference_model, imputation_model, conc_units, meta, tox_analytes}{Forwarded to \code{\link[=fit_target_model]{fit_target_model()}}.}

\item{daily_long}{Forward-filled daily chemistry for the site; supplies the
date grid (\code{qdates}) and co-analyte values.}
}
\value{
A named list (\code{fdm}) or \code{NULL} on model failure.
}
\description{
Calls \code{\link[=fit_target_model]{fit_target_model()}} once, builds the static scaffolding needed for
per-draw prediction, and precomputes the OU bridge Cholesky factors for
every analyte over the full daily date grid.  The result ("fitted daily
model", \code{fdm}) is the input to \code{\link[=.predict_daily_tox]{.predict_daily_tox()}}; call
\code{.predict_daily_tox(fdm)} once for point mode or N times (with a perturbed
target model and OU-sampled \code{eps_paths}) for draw mode.
}
\keyword{internal}
