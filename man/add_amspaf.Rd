% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mspaf.R
\name{add_amspaf}
\alias{add_amspaf}
\title{Compute the Adjusted multi-substance PAF (AmsPAF) for water quality samples}
\usage{
add_amspaf(
  df,
  reference = NULL,
  analyte_metadata = NULL,
  method = c("multi", "anzecc"),
  guideline_dir = getOption("leachatetools.guideline_dir"),
  min_analytes = 3,
  ref_summary = c("geom_mean", "median", "arith_mean", "p80", "p90", "p95"),
  require_temperature = TRUE
)
}
\arguments{
\item{df}{Long-format monitoring dataframe. Required columns:
\code{sample_id}, \code{site_id}, \code{analyte}, \code{value} (concentrations in
µg/L). Optional but recommended: \code{datetime} (propagated to AmsPAF
rows if present), \code{detected} (assumed \code{TRUE} if absent), \code{imputed}
(logical; if present, \code{n_analytes_imputed} is populated in output).
Driver analytes needed for chemistry normalisation (e.g. pH, DOC) should
be present as rows in \code{df}.}

\item{reference}{Background reference chemistry for the ARA adjustment.
Accepts three forms:
\itemize{
\item A \code{prepared_reference} object from \code{\link[=prepare_reference]{prepare_reference()}} —
normalisation has already been applied; used directly.
\item A raw long-format data frame (same schema as \code{df}) — will be
passed to \code{\link[=prepare_reference]{prepare_reference()}} internally.
\item \code{NULL} (default) — no ARA adjustment; raw concentrations assessed
directly against SSDs.
}}

\item{analyte_metadata}{Data frame of analyte metadata, or \code{NULL} to load
the bundled \code{inst/extdata/anzecc_analyte_metadata.csv}. Passed to
\code{\link[=prepare_reference]{prepare_reference()}} and \code{\link[=derive_ssd_params]{derive_ssd_params()}}.}

\item{method}{SSD method passed to \code{\link[=ssd_hc50]{ssd_hc50()}} and \code{\link[=ssd_paf]{ssd_paf()}}.
\code{"multi"} (default) fits all 6 BCANZ distributions and model-averages;
\code{"anzecc"} uses the per-analyte distribution matching the original ANZG
derivation.}

\item{guideline_dir}{Path to the "guideline data" folder containing ANZG
XLSX files. Falls back to \code{getOption("leachatetools.guideline_dir")}.}

\item{min_analytes}{Minimum number of analytes with fitted SSDs required
to compute AmsPAF for a sample. Default \code{3}.}

\item{ref_summary}{Summary statistic for the reference distribution when
\code{reference} is a raw data frame.  Passed through to
\code{\link[=prepare_reference]{prepare_reference()}}.  Default \code{"geom_mean"} — the maximum-likelihood
central tendency for log-normal concentrations, and a PICT-consistent
estimate of the "typical" exposure the resident community has adapted
to.  Other options: \code{"median"}, \code{"arith_mean"}, \code{"p80"}, \code{"p90"},
\code{"p95"}.}

\item{require_temperature}{Logical (default \code{TRUE}). When \code{TRUE}, any sample
that reports an \code{NH3-N} measurement \strong{must} also carry a water
\code{temperature} row (the ammonia un-ionised-fraction normalisation is
undefined without it); a missing temperature is a hard error rather than a
silent drop of ammonia. Supply temperature via direct measurement, or
derive it with \code{\link[=estimate_water_temp]{estimate_water_temp()}} (optionally fed by
\code{\link[=get_silo_air_temp]{get_silo_air_temp()}}). Set \code{FALSE} only for datasets that do not assess
ammonia.}
}
\value{
The input \code{df} with AmsPAF rows appended. Each AmsPAF row carries:
\code{value} (AmsPAF as a percentage, 0–100+), \code{detected = TRUE},
\code{analyte = "AmsPAF"}, \code{n_analytes_used} (integer),
\code{n_analytes_imputed} (integer, 0 if \code{imputed} column absent),
\code{dominant_analyte} (character), \code{max_paf} (numeric),
\code{analyte_pafs} (list column of per-analyte diagnostic tibbles, each with
\code{analyte}, \code{C_adj}, \code{PAF}, \code{moa_group}, and \code{ref_source} — one of
\code{"disabled"}, \code{"matched"}, \code{"unmatched"} recording how the ARA reference
was resolved for that analyte).

Tier breaks are not provided by this package — AmsPAF is a continuous
risk metric and the threshold at which a community is "impacted"
depends on the assessment context.  See
\code{vignette("chronic-amspaf-interpretation")} for guidance.
}
\description{
Appends AmsPAF rows to a long-format water quality dataframe. AmsPAF
estimates the fraction of aquatic species potentially affected by the
combined toxicant mixture, adjusted for local geogenic background via the
Added Risk Approach. See the file-level header for full methodological
detail.
}
\details{
The function accepts either per-sample or chronic-integrated chemistry (from
\code{\link[=time_weighted_aggregate]{time_weighted_aggregate()}}). It does not need to know which — the
distinction is entirely in the input data. Similarly, \code{reference} may be a
raw long-format chemistry data frame or a pre-built \code{\link[=prepare_reference]{prepare_reference()}}
object.
}
\examples{
\donttest{
# Per-sample multi-substance PAF for the impacted site, with local
# background subtracted via the reference site. Uses the bundled SSD data.
ds  <- subset(leachate_demo, site_id == "downstream")
ref <- subset(leachate_demo, site_id == "reference")
out <- add_amspaf(ds, reference = ref)
subset(out, analyte == "AmsPAF", c("sample_id", "value"))
}
}
\references{
De Zwart D, Posthuma L (2005) Environmental Toxicology and Chemistry
24(10):2665-2676.
}
\seealso{
\code{\link[=ssd_paf]{ssd_paf()}}, \code{\link[=ssd_hc50]{ssd_hc50()}}, \code{\link[=prepare_reference]{prepare_reference()}},
\code{\link[=time_weighted_aggregate]{time_weighted_aggregate()}}, \code{\link[=prescreen_analytes]{prescreen_analytes()}}, \code{\link[=impute_chemistry]{impute_chemistry()}}
}
