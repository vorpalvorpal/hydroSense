% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mspaf.R
\name{.amspaf_add_paf}
\alias{.amspaf_add_paf}
\title{Add per-analyte PAF to adjusted tox rows, batched per analyte}
\usage{
.amspaf_add_paf(tox, ssd_params, method, guideline_dir)
}
\arguments{
\item{tox}{Adjusted tox rows from \code{\link[=.amspaf_adjust]{.amspaf_adjust()}} (needs \code{analyte},
\code{C_adj}); may span multiple samples.}

\item{ssd_params}{Tibble from \code{\link[=derive_ssd_params]{derive_ssd_params()}} (uses \code{analyte}, \code{fit}).}

\item{method}{SSD method (NULL-fit fallback only).}

\item{guideline_dir}{Path to ANZG XLSX folder (NULL-fit fallback only).}
}
\value{
\code{tox} with a numeric \code{PAF} column added (proportion, 0–1).
}
\description{
Evaluates each analyte's SSD once across \strong{all} its \code{C_adj} values in a
single vectorised \code{\link[ssdtools:ssd_hp]{ssdtools::ssd_hp()}} call (via \code{\link[=.ssd_paf_vec]{.ssd_paf_vec()}}), rather
than one \code{\link[=ssd_paf]{ssd_paf()}} call per row.  The fitted SSD object is taken from the
\code{fit} list-column of \code{ssd_params} (loaded once in \code{\link[=derive_ssd_params]{derive_ssd_params()}}).
}
\keyword{internal}
