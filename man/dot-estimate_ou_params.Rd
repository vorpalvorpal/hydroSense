% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ou_bridge.R
\name{.estimate_ou_params}
\alias{.estimate_ou_params}
\title{Method-of-moments OU parameter estimation from an anchor residual series}
\usage{
.estimate_ou_params(anchor_dates, anchor_S, scale = 1)
}
\arguments{
\item{anchor_dates}{Date (or Date-coercible) vector of anchor dates.}

\item{anchor_S}{Numeric; residual state S at each anchor, same length.}

\item{scale}{Positive multiplier applied to γ and σ² (envelope-width
tuning knob, default 1).  θ is unchanged (correlation structure
preserved).}
}
\value{
Named list: \code{theta} (1/day), \code{sigma2} (\link[=S<c2><b2>/day]{S²/day}),
\code{gamma} (\link[=S<c2><b2>]{S²}), \code{degenerate} (logical).
}
\description{
Estimates the Ornstein-Uhlenbeck parameters θ (mean-reversion rate in
units of 1/day), σ² (diffusion coefficient in \link[=S<c2><b2>/day]{S²/day}), and γ (marginal
variance in \link[=S<c2><b2>]{S²}) from an irregularly-spaced series of residual-state S
values at grab-sample anchor dates.
}
\details{
\strong{Estimators:}
\itemize{
\item γ̂ = sample variance of anchor S values.
\item σ̂² = median\link[=(<ce><94>S)<c2><b2>/<ce><94>t]{(ΔS)²/Δt} over consecutive anchor pairs (robust quadratic
variation; median is resistant to outlier jumps at distant anchor pairs).
\item θ̂ = σ̂²/(2γ̂)  (from the OU marginal-variance identity γ = σ²/(2θ)).
}

Returns \code{degenerate = TRUE} when fewer than two finite anchors are
available, when S is essentially constant (γ̂ ≈ 0), or when consecutive
anchors show no detectable diffusion (σ̂² ≈ 0).  A degenerate result means
\code{\link[=.ou_bridge_draw]{.ou_bridge_draw()}} returns a zero path (no added uncertainty).
}
\keyword{internal}
