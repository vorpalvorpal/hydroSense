% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kalman_bridge.R
\name{.loo_coverage_series}
\alias{.loo_coverage_series}
\title{Leave-one(-block)-out coverage of one residual series}
\usage{
.loo_coverage_series(
  anchor_dates,
  anchor_S,
  interval = 0.9,
  block = 1L,
  n_fit_min = 8L,
  scale = 1
)
}
\arguments{
\item{anchor_dates, anchor_S}{The series.}

\item{interval}{Nominal coverage (default 0.9).}

\item{block}{Number of consecutive anchors held out per fold (default 1).}

\item{n_fit_min, scale}{Passed through to the smoother.}
}
\value{
list(coverage, mean_width, n, n_held). \code{coverage}/\code{mean_width} are
\code{NA} when too few anchors remain to fit.
}
\description{
Holds out each anchor (or each run of \code{block} consecutive anchors), refits the
smoother on the rest, and checks whether the held-out value falls inside the
predictive interval (state posterior + observation noise). Plain LOO mainly
tests the pinch zone; \code{block >= 2} probes mid-gap width.
}
\keyword{internal}
