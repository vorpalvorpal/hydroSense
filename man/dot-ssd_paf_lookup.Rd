% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mspaf.R
\name{.ssd_paf_lookup}
\alias{.ssd_paf_lookup}
\title{Resolve or build a spline-based PAF lookup closure for one analyte}
\usage{
.ssd_paf_lookup(analyte, method, fit, guideline_dir)
}
\arguments{
\item{analyte}{Analyte name string.}

\item{method}{SSD method string (e.g. \code{"multi"}, \code{"anzecc"}).}

\item{fit}{\code{fitdists} object or \code{NULL}.  Required for runtime builds.}

\item{guideline_dir}{Path to ANZG XLSX folder, or \code{NULL} for shipped tables.}
}
\value{
A \code{splinefun} closure, or \code{NULL} if the lookup cannot be built.
}
\description{
Returns a \code{stats::splinefun} closure that maps \code{log10(conc)} to PAF.
Shipped tables (\verb{NULL guideline_dir} + known method/analyte) are loaded
from \code{inst/extdata/ssd_paf_lookup.qs2} and cached in the session.
Runtime tables are built adaptively and likewise cached.
}
\keyword{internal}
