% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{impute_chemistry}
\alias{impute_chemistry}
\title{Impute missing and BDL chemistry using a fitted imputation model}
\usage{
impute_chemistry(
  df,
  model,
  apply_hurdles = TRUE,
  bdl_cap = TRUE,
  return = c("point", "draws"),
  ndraws = NULL,
  batch_size = NULL
)
}
\arguments{
\item{df}{Long-format chemistry data frame (same schema as used for fitting).}

\item{model}{Fitted model from \code{\link[=fit_imputation_model]{fit_imputation_model()}}.}

\item{apply_hurdles}{Logical.  Apply each group's presence hurdle?  Default
\code{TRUE}.  When \code{FALSE}, every sample is eligible for every group.}

\item{bdl_cap}{Logical.  Cap imputed BDL values at the original detection
limit?  Default \code{TRUE}.  Applied to all methods: an imputed below-detection
value should never exceed its detection limit.}

\item{return}{\code{"point"} (default) for posterior mean per cell; \code{"draws"} for
one row per (sample × analyte × draw).}

\item{ndraws}{Integer or \code{NULL}.  Use only this many posterior draws for
prediction (subsampled).  \code{NULL} (default) uses all draws.  Lowering it
reduces memory/time, at some cost to interval precision.}

\item{batch_size}{Integer or \code{NULL}.  Predict eligible samples in batches of
this many rows to bound peak memory (important for \code{"rescor_mi"}, whose
\code{mi()} prediction is memory-heavy).  \code{NULL} (default) predicts all at once.}
}
\value{
\code{df} with BDL and missing cells in every fitted group
replaced by posterior mean estimates, plus columns:
\itemize{
\item \code{imputed} (logical) — \code{TRUE} for filled cells
\item \code{imputed_kind} — \code{"observed"}, \code{"censored_left"}, or \code{"missing"}
}

When \code{bdl_cap = TRUE} and any imputed BDL cell exceeded its detection
limit, a per-cell audit of the cap activations is attached as the
\code{"bdl_cap_summary"} attribute; retrieve it with \code{\link[=bdl_cap_summary]{bdl_cap_summary()}}.
}
\description{
Applies the models fitted by \code{\link[=fit_imputation_model]{fit_imputation_model()}} to \code{df}, returning
posterior estimates for below-detection-limit (BDL) and missing observations
in every fitted group.
}
\details{
\strong{Completing the panel}

For each eligible sample (see \emph{Hurdles}), every target analyte the group
models is filled: BDL cells are replaced with their posterior estimate, and
analytes that are \strong{entirely absent} for a sample gain a new
model-anchored row (\code{imputed_kind = "missing"}, \code{detected = TRUE}). This is
what lets a well-sampled analyte lift a sparsely-sampled one — e.g. a sample
with Zn but no Cu gains a Cu row predicted from the fitted Cu–Zn
relationship. Fabricated rows carry the originating sample's \code{site_id},
\code{datetime}, and any other sample-level columns.

Fabricated rows are model predictions, not measurements. With
\code{return = "draws"} the imputation-model uncertainty propagates through the
draw carrier; with \code{return = "point"} the single anchor enters any
downstream model as if observed, which can understate that model's
uncertainty. Prefer \code{return = "draws"} when the imputed values feed a
further model (e.g. the reference/target GAMs).

\strong{Hurdles}

Each fitted group may carry a presence hurdle (see \code{\link[=impute_group]{impute_group()}}).  When
\code{apply_hurdles = TRUE}, imputed values for a group are only returned for
samples carrying at least one of that group's hurdle analytes (detected or
BDL) — e.g. under the leachate preset, a sample with no metals recorded is
not given imputed metals, because a leachate metal pulse may simply not have
arrived at that location yet.  Samples failing a hurdle pass through
unchanged (non-imputed values preserved; BDL values remain flagged as BDL).
}
\examples{
\dontrun{
model <- fit_imputation_model(monitoring_long)
# Point estimates (default), or full posterior draws with return = "draws":
imputed <- impute_chemistry(monitoring_long, model, return = "point")
}
}
\seealso{
\code{\link[=fit_imputation_model]{fit_imputation_model()}}, \code{\link[=bdl_cap_summary]{bdl_cap_summary()}}
}
