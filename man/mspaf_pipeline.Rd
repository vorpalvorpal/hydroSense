% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mspaf_pipeline.R
\name{mspaf_pipeline}
\alias{mspaf_pipeline}
\title{Run the full daily msPAF pipeline end to end}
\usage{
mspaf_pipeline(
  target,
  reference,
  hydro = NULL,
  impute = TRUE,
  impute_on = c("reference", "target"),
  required_vars = c("pH", "EC"),
  impute_groups = NULL,
  impute_seed = NULL,
  reference_args = list(),
  daily_args = list()
)
}
\arguments{
\item{target}{Long-format target-site chemistry (same schema as
\code{\link[=mspaf_daily]{mspaf_daily()}}'s \code{df}): \code{sample_id}, \code{site_id}, \code{datetime}, \code{analyte},
\code{value}, \code{detected}.}

\item{reference}{Long-format reference-site chemistry passed to
\code{\link[=fit_reference_model]{fit_reference_model()}}.}

\item{hydro}{Optional hydrology series forwarded to \code{\link[=fit_reference_model]{fit_reference_model()}}.}

\item{impute}{Logical. Fit and apply a Bayesian imputation model? Default
\code{TRUE}. When \code{FALSE}, no imputation model is fitted and \code{NULL} is passed
downstream (identical to chaining the functions by hand without one).}

\item{impute_on}{Which chemistry to fit the imputation model on:
\code{"reference"} (default, matching the documented design) or \code{"target"}.}

\item{required_vars}{Passed to \code{\link[=fit_imputation_model]{fit_imputation_model()}}. Default
\code{c("pH", "EC")}.}

\item{impute_groups}{Optional \code{groups} for \code{\link[=fit_imputation_model]{fit_imputation_model()}}; \code{NULL}
(default) uses \code{\link[=leachate_impute_groups]{leachate_impute_groups()}}.}

\item{impute_seed}{Optional integer seed for the imputation fit (forwarded to
\code{brms::brm()} via \code{\link[=fit_imputation_model]{fit_imputation_model()}}) so the default-on path is
reproducible.}

\item{reference_args, daily_args}{Named lists of additional arguments
forwarded to \code{\link[=fit_reference_model]{fit_reference_model()}} and \code{\link[=mspaf_daily]{mspaf_daily()}} respectively. Do
not include arguments the orchestrator sets itself (\code{reference}/\code{hydro}/
\code{imputation_model} for the reference fit; \code{df}/\code{reference_model}/
\code{imputation_model} for the daily call). \code{daily_args} may override the
\code{interpolation = "model"} default.}
}
\value{
The \code{\link[=mspaf_daily]{mspaf_daily()}} result, with the fitted models attached as
attributes \code{"reference_model"} and \code{"imputation_model"} (the latter \code{NULL}
when \code{impute = FALSE} or the fit was skipped). Note that most \pkg{dplyr}
verbs drop attributes, so read them off the returned frame directly.
}
\description{
Orchestrates the three steps a daily msPAF analysis usually chains by hand:
optionally fit a Bayesian imputation model (\code{\link[=fit_imputation_model]{fit_imputation_model()}}), fit
the reference/impact model (\code{\link[=fit_reference_model]{fit_reference_model()}}), then compute the daily
multi-substance PAF (\code{\link[=mspaf_daily]{mspaf_daily()}}) for the target site. The fitted
imputation model is threaded into \strong{both} downstream steps so that
below-detection and entirely-absent analytes are filled before the toxicity
calculation.
}
\details{
\strong{Imputation is on by default here.} The individual functions treat
imputation as opt-in (you must fit a model and pass it). This orchestrator
flips that default: with \code{impute = TRUE} it fits an imputation model and
applies it throughout. This is convenient but \strong{not free of consequences} —
imputing below-detection cells (replacing detection-limit values with
modelled sub-limit estimates) and fabricating entirely-absent analytes
generally \emph{moves} the msPAF result. Whether that is appropriate is a
missingness (MAR/MNAR) modelling judgement, not a neutral default. Set
\code{impute = FALSE} to reproduce the non-imputed behaviour of calling
\code{\link[=fit_reference_model]{fit_reference_model()}} and \code{\link[=mspaf_daily]{mspaf_daily()}} directly.
}
\examples{
\dontrun{
demo <- leachate_demo()
out <- mspaf_pipeline(
  target = subset(demo, site_id == "downstream"),
  reference = subset(demo, site_id == "reference"),
  daily_args = list(require_temperature = FALSE, ndraws = 50L, seed = 1L)
)
}
}
\seealso{
\code{\link[=fit_imputation_model]{fit_imputation_model()}}, \code{\link[=fit_reference_model]{fit_reference_model()}}, \code{\link[=mspaf_daily]{mspaf_daily()}}
}
