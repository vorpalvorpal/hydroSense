% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/route-c.R
\name{.fit_group_model_factor}
\alias{.fit_group_model_factor}
\title{Fit the Route C two-stage censored factor group model}
\usage{
.fit_group_model_factor(
  target_analytes,
  safe_analytes,
  base,
  pc_wide,
  pc_cols,
  log_floors,
  iter,
  warmup,
  chains,
  cores,
  group_name = "group",
  k = NULL,
  ...
)
}
\description{
Stage 1: per-analyte \code{mgcv::gam(lv ~ s(PC1) + ...)} on detected
observations gives the spline mean \code{mu_j(X_i)}; detected residuals and BDL
censoring bounds (\code{log(DL) - mu_j}) are computed from it. Stage 2: a Stan
program (\code{inst/stan/factor_censored.stan}) fits a rank-\code{k} factor model on
those residuals, with BDL cells as upper-bounded latent parameters (proper
left-censoring, jointly with the factor and covariance).
}
\keyword{internal}
