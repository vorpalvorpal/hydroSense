% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mspaf.R
\name{compute_ca_group_mspaf}
\alias{compute_ca_group_mspaf}
\title{Compute msPAF for a single Concentration Addition group}
\usage{
compute_ca_group_mspaf(group_data)
}
\arguments{
\item{group_data}{Tibble with columns \code{C_adj}, \code{hc50}, \code{sigma}, \code{moa_group};
one row per analyte in the CA group.}
}
\value{
msPAF as a proportion (not percentage).
}
\description{
Implements the multispecies Concentration Addition model of De Zwart &
Posthuma (2005, Environ Toxicol Chem 24(10):2665-2676), eq. 6: hazard units
(here \code{TU = C_adj / HC50}) are summed across the components sharing a toxic
mode of action, and the combined proportion affected is the log-normal CDF
evaluated at \verb{log10(ΣTU)} with a mixture slope equal to the \strong{arithmetic
mean of the component slopes} (their \verb{β̄_TMoA}; in the normal-CDF form the
slope is the standard deviation \verb{σ̄ = mean(σ)}):
}
\details{
\deqn{msPAF_{CA} = \Phi\!\left( \frac{\log_{10}(\sum TU)}{\bar{\sigma}} \right)}

Note this is a plain (unweighted) average of the per-analyte sigmas, per the
primary source — NOT a TU-weighted or variance-style combination.  A
single-component group therefore reduces exactly to that component's own SSD.
}
\keyword{internal}
