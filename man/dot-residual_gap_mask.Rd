% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kalman_bridge.R
\name{.residual_gap_mask}
\alias{.residual_gap_mask}
\title{Flag grid days where the residual posterior variance has ballooned in a gap}
\usage{
.residual_gap_mask(sm, anchor_dates, tol = 0.05)
}
\arguments{
\item{sm}{A smoother list with \code{grid_dates} and \code{var} (e.g. from
\code{\link[=.residual_smoother]{.residual_smoother()}} or the per-analyte scaffold).}

\item{anchor_dates}{Grab/anchor dates; used to locate the at-anchor floor.}

\item{tol}{Fraction of the floor-to-peak variance range above the floor at
which a day is declared in-gap (default 0.05).}
}
\value{
Logical vector along \code{sm$grid_dates}; \code{logical(0)} for an empty grid.
}
\description{
The simulation-smoother posterior variance is small at (and near) the grab
anchors and inflates between them, peaking in the middle of long gaps
(Brownian-bridge shape). A grid day counts as "in-gap" when its variance has
risen meaningfully above the at-anchor floor — i.e. more than a fraction
\code{tol} of the way from that floor to the series' peak variance. This is the
set of days on which the informative envelope (residual frozen at its
posterior mean) may differ from the ignorable envelope (residual drawn);
everywhere else the two coincide. Observation days, a densely-sampled series
with no real gaps, and a degenerate (zero-variance) series all return \code{FALSE}
throughout.
}
\details{
The floor is taken at the anchor positions (the grab days carry only the
tiny observation-noise variance); using a \emph{relative} threshold against the
floor-to-peak range makes the test scale-free, so it collapses to "no gaps"
automatically when sampling is dense.
}
\keyword{internal}
