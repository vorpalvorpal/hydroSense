% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coupling.R
\name{.coupled_residual_draws}
\alias{.coupled_residual_draws}
\title{Draw cross-analyte correlated Kalman residual trajectories}
\usage{
.coupled_residual_draws(
  smoothers,
  modelled,
  ndraws,
  cor_R,
  cor_analytes = rownames(cor_R),
  seed = NULL
)
}
\arguments{
\item{smoothers}{Named list by analyte.  Each element is a list with
\code{grid_dates} (Date vector), \code{mean} (numeric vector, KFS posterior mean),
and \code{draw_model} (KFAS model from \code{\link[=.build_kalman_model]{.build_kalman_model()}}, or \code{NULL} for
a degenerate / non-modelled analyte).}

\item{modelled}{Character vector of analyte names to include in the output
(subset of \code{names(smoothers)}).}

\item{ndraws}{Positive integer; number of draws to generate.}

\item{cor_R}{\verb{[p × p]} numeric positive-definite correlation matrix (no
dimnames required).  Typically the \verb{$R} component from
\code{\link[=.anchor_residual_cor]{.anchor_residual_cor()}}.}

\item{cor_analytes}{Character vector giving the analyte order for the rows
and columns of \code{cor_R}.  Defaults to \code{rownames(cor_R)} when \code{cor_R} has
dimnames; must be supplied explicitly otherwise.}

\item{seed}{Integer or \code{NULL}.  When non-\code{NULL}, \code{set.seed(seed)} is called
at the start of the function so results are reproducible.  In
\code{amspaf_daily()}, the seed is set by the caller (via its own \code{seed}
argument) before this function is invoked, so \code{seed = NULL} is used there.}
}
\value{
Named list by analyte (same order as \code{modelled}).  Each element is
\verb{list(grid_dates = <Date>, draws = <matrix [n_grid × ndraws]>)}.
Analytes with \code{draw_model = NULL} receive flat draws equal to their KFS
posterior mean (the degenerate path).  Analytes with empty \code{grid_dates}
receive \code{draws = NULL}.
}
\description{
Generates \code{ndraws} draws from each analyte's Durbin–Koopman simulation
smoother, with innovations correlated \strong{across analytes} according to the
\verb{[p × p]} correlation sub-matrix \code{cor_R}.  Each analyte's per-analyte
marginal distribution is unchanged by the correlation (DK identity);
only the \emph{joint} distribution across analytes is affected.
}
\details{
\subsection{Statistical justification}{

\strong{DK identity} (Durbin & Koopman 2002, §4): the draw
\deqn{\tilde{\alpha} = \hat{\alpha} + (\alpha^+ - L y^+)}
guarantees each analyte's marginal distribution is unchanged when the
process innovations \eqn{\eta^+} are correlated: the smoother gain \code{L} is
precomputed on the actual data, and the correction \eqn{\alpha^+ - L y^+}
has expectation zero.  Correlating \eqn{\eta^+} across analytes widens only
the \emph{joint} distribution (i.e. the combined AmsPAF interval).

\strong{Cholesky coupling}: \code{Z_raw \%*\% U_chol} (where \code{U_chol = chol(R)}, upper
triangular) gives rows with covariance \code{R} when rows of \code{Z_raw} are iid
\eqn{N(0, I_p)}.  The same \code{U_chol} is applied to both the process
innovations and the initial-state draws so that the coupling is coherent
along the entire trajectory.

\strong{Independent observation noise}: \code{eps_std} is drawn independently per
analyte because measurement errors from separate grab analyses are
uncorrelated even when the true concentrations co-move.

\strong{Ragged grids}: \code{match(grid_a, U)} slices the union-grid field to each
analyte's own clipped grid.  Days where an analyte has no entry (outside its
grab span) have no field element — no coupling on those days, which is
correct.
}
}
\references{
Durbin J, Koopman SJ (2002). A simple and efficient simulation smoother
for state space time series analysis. \emph{Biometrika} \strong{89}(3), 603–616.
\url{https://doi.org/10.1093/biomet/89.3.603}
}
\keyword{internal}
