% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chronic.R
\name{expand_focal_dates}
\alias{expand_focal_dates}
\title{Generate a sequence of focal dates for chronic AmsPAF computation}
\usage{
expand_focal_dates(start, end, by = "day")
}
\arguments{
\item{start}{Start date (character \code{"YYYY-MM-DD"} or \code{Date}).}

\item{end}{End date (character \code{"YYYY-MM-DD"} or \code{Date}).}

\item{by}{Increment. Passed to \code{\link[base:seq.Date]{base::seq.Date()}}. Common values:
\code{"day"}, \code{"week"}, \code{"month"}. Default \code{"day"}.}
}
\value{
A \code{Date} vector from \code{start} to \code{end} at the specified increment.
}
\description{
A thin convenience wrapper around \code{\link[base:seq.Date]{base::seq.Date()}} for generating the
\code{focal_dates} vector passed to \code{\link[=time_weighted_aggregate]{time_weighted_aggregate()}}.  The most
common use is a daily sequence for time-series analysis.
}
\examples{
\dontrun{
# Daily sequence for 2024–2025
focal_dates <- expand_focal_dates("2024-01-01", "2025-12-31", by = "day")

chr_chem <- time_weighted_aggregate(imp, focal_dates = focal_dates)
}

}
