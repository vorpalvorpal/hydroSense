% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mspaf.R
\name{add_mspaf}
\alias{add_mspaf}
\title{Compute the Adjusted multi-substance PAF (msPAF) for water quality samples}
\usage{
add_mspaf(
  df,
  reference = NULL,
  analyte_metadata = NULL,
  method = c("multi", "anzecc"),
  guideline_dir = getOption("hydroSense.guideline_dir"),
  min_analytes = 3,
  ref_summary = c("geom_mean", "median", "arith_mean", "p80", "p90", "p95"),
  conc_units = NULL,
  require_temperature = TRUE,
  tau = 90,
  tau_units = "day",
  window = 365,
  window_units = "day",
  return = c("summary", "draws"),
  interval = 0.9,
  central = c("median", "mean")
)
}
\arguments{
\item{df}{Long-format monitoring dataframe. Required columns:
\code{sample_id}, \code{site_id}, \code{analyte}, \code{value}. Toxicant concentrations must
ultimately be in µg/L for SSD lookup; supply them either via a
\code{units.analyte} column (one unit string per row, e.g. \code{"mg/L"}) or via the
\code{conc_units} argument (applied uniformly to all SSD-eligible rows). Optional
but recommended: \code{datetime} (propagated to msPAF rows if present),
\code{detected} (assumed \code{TRUE} if absent), \code{imputed} (logical; if present,
\code{n_analytes_imputed} is populated in output). Driver analytes needed for
chemistry normalisation (e.g. pH, DOC) should be present as rows in \code{df}.}

\item{reference}{Background reference chemistry for the ARA adjustment.
Accepts four forms:
\itemize{
\item A \code{reference_model} from \code{\link[=fit_reference_model]{fit_reference_model()}} — contemporaneous
(temporal) ARA; the model predicts what the reference site would show
at each target sample's exact moment using hydrology and seasonality.
\item A \code{prepared_reference} object from \code{\link[=prepare_reference]{prepare_reference()}} —
normalisation has already been applied; used directly (static ARA).
\item A raw long-format data frame (same schema as \code{df}) — will be
passed to \code{\link[=prepare_reference]{prepare_reference()}} internally (static ARA).
\item \code{NULL} (default) — no ARA adjustment; raw concentrations assessed
directly against SSDs.
}}

\item{analyte_metadata}{Data frame of analyte metadata, or \code{NULL} to load
the bundled \code{inst/extdata/anzecc_analyte_metadata.csv}. Passed to
\code{\link[=prepare_reference]{prepare_reference()}} and \code{\link[=derive_ssd_params]{derive_ssd_params()}}.}

\item{method}{SSD method passed to \code{\link[=ssd_hc50]{ssd_hc50()}} and \code{\link[=ssd_paf]{ssd_paf()}}.
\code{"multi"} (default) fits all 6 BCANZ distributions and model-averages;
\code{"anzecc"} uses the per-analyte distribution matching the original ANZG
derivation.}

\item{guideline_dir}{Path to the "guideline data" folder containing ANZG
XLSX files. Falls back to \code{getOption("hydroSense.guideline_dir")}.}

\item{min_analytes}{Minimum number of analytes with fitted SSDs required
to compute msPAF for a sample. Default \code{3}.}

\item{ref_summary}{Summary statistic for the reference distribution when
\code{reference} is a raw data frame.  Passed through to
\code{\link[=prepare_reference]{prepare_reference()}}.  Default \code{"geom_mean"} — the maximum-likelihood
central tendency for log-normal concentrations, and a PICT-consistent
estimate of the "typical" exposure the resident community has adapted
to.  Other options: \code{"median"}, \code{"arith_mean"}, \code{"p80"}, \code{"p90"},
\code{"p95"}.}

\item{conc_units}{Character. Unit string (e.g. \code{"mg/L"}, \code{"ug/L"}) applied
uniformly to all SSD-eligible rows in \code{df} when \code{df} has no
\code{units.analyte} column. Ignored when \code{df} already carries \code{units.analyte}.
Required when \code{df} lacks \code{units.analyte} and toxicant concentrations are
not already in µg/L.}

\item{require_temperature}{Logical (default \code{TRUE}). When \code{TRUE}, any sample
that reports an \code{NH3-N} measurement \strong{must} also carry a water
\code{temperature} row (the ammonia un-ionised-fraction normalisation is
undefined without it); a missing temperature is a hard error rather than a
silent drop of ammonia. Supply temperature via direct measurement, or
derive it with \code{\link[=estimate_water_temp]{estimate_water_temp()}} (optionally fed by
\code{\link[=get_silo_air_temp]{get_silo_air_temp()}}). Set \code{FALSE} only for datasets that do not assess
ammonia.}

\item{tau, tau_units}{Exponential-decay half-life for chronic window
integration when \code{reference} is a \code{reference_model}.  Default \code{90} days.}

\item{window, window_units}{Look-back window length for chronic integration.
Default \code{365} days.}

\item{return}{Output mode for draw-carrier input (see \code{\link[=summarise_draws]{summarise_draws()}}).
\code{"summary"} (default) collapses posterior draws to a central estimate plus
a credible interval (\code{value}, \code{value_lower}, \code{value_upper}, \code{n_draws});
\code{"draws"} returns the raw per-draw msPAF rows (\verb{draw_id 1..N}) for
external risk models or further composition (e.g. into
\code{\link[=time_weighted_aggregate]{time_weighted_aggregate()}}).  For point (non-draw) input both modes
return byte-identical output with no interval columns.}

\item{interval}{Width of the credible interval when \code{return = "summary"}.
Default \code{0.90} (5th/95th percentile bounds).}

\item{central}{Central-tendency statistic when \code{return = "summary"}:
\code{"median"} (default) or \code{"mean"}.}
}
\value{
The input \code{df} with msPAF rows appended. Each msPAF row carries:
\code{value} (msPAF as a percentage, 0–100+), \code{detected = TRUE},
\code{analyte = "msPAF"}, \code{n_analytes_used} (integer),
\code{n_analytes_imputed} (integer, 0 if \code{imputed} column absent),
\code{dominant_analyte} (character), and \code{max_paf} (numeric).

The result carries two attributes (read them before further dplyr wrangling,
which drops attributes): \code{"analyte_pafs"} — the per-analyte PAF breakdown as
a flat tibble (\code{site_id}, \code{sample_id}, \code{draw_id} in draws mode, \code{analyte},
\code{C_adj}, \code{PAF}, \code{moa_group}, \code{ref_source}), retrieved with \code{\link[=analyte_pafs]{analyte_pafs()}};
and \code{"ara_summary"} — per-(sample × analyte) ARA diagnostics, retrieved with
\code{\link[=ara_summary]{ara_summary()}}. (\code{analyte_pafs} was formerly a per-row list-column; it is
now a flat attribute — issue #30.)

Tier breaks are not provided by this package — msPAF is a continuous
risk metric and the threshold at which a community is "impacted"
depends on the assessment context.  See
\code{vignette("chronic-mspaf-interpretation")} for guidance.
}
\description{
Appends msPAF rows to a long-format water quality dataframe. msPAF
estimates the fraction of aquatic species potentially affected by the
combined toxicant mixture, adjusted for local geogenic background via the
Added Risk Approach. See the file-level header for full methodological
detail.
}
\details{
The function accepts either per-sample or chronic-integrated chemistry (from
\code{\link[=time_weighted_aggregate]{time_weighted_aggregate()}}). It does not need to know which — the
distinction is entirely in the input data. Similarly, \code{reference} may be a
raw long-format chemistry data frame or a pre-built \code{\link[=prepare_reference]{prepare_reference()}}
object.
}
\examples{
\donttest{
# Per-sample multi-substance PAF for the impacted site, with local
# background subtracted via the reference site. Uses the bundled SSD data.
demo <- leachate_demo()
ds  <- subset(demo, site_id == "downstream")
ref <- subset(demo, site_id == "reference")
out <- add_mspaf(ds, reference = ref)
subset(out, analyte == "msPAF", c("sample_id", "value"))
}
}
\references{
De Zwart D, Posthuma L (2005) Environmental Toxicology and Chemistry
24(10):2665-2676.
}
\seealso{
\code{\link[=ssd_paf]{ssd_paf()}}, \code{\link[=ssd_hc50]{ssd_hc50()}}, \code{\link[=prepare_reference]{prepare_reference()}},
\code{\link[=fit_reference_model]{fit_reference_model()}}, \code{\link[=ara_summary]{ara_summary()}},
\code{\link[=time_weighted_aggregate]{time_weighted_aggregate()}}, \code{\link[=prescreen_analytes]{prescreen_analytes()}}, \code{\link[=impute_chemistry]{impute_chemistry()}}
}
