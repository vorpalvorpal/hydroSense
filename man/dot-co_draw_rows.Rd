% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mspaf_daily.R
\name{.co_draw_rows}
\alias{.co_draw_rows}
\title{Build per-draw co-analyte rows for the synthetic frame (S7, G2)}
\usage{
.co_draw_rows(daily_long_exact, co_pert_split, draw_id)
}
\arguments{
\item{daily_long_exact}{Rows of the forward-filled daily chemistry that
belong to the non-modelled subset (co-analytes + unmodelled toxicants).}

\item{co_pert_split}{Named list (date-string → data.frame(analyte, value))
from \code{\link[=.perturb_co_split]{.perturb_co_split()}}.}

\item{draw_id}{Integer draw index to assign to the output rows.}
}
\value{
A copy of \code{daily_long_exact} with perturbed values and \code{draw_id}
set to \code{draw_id}.
}
\description{
Creates a copy of the exact co-analyte rows from \code{daily_long_exact} with
values replaced by the corresponding perturbed values from \code{co_pert_split}
(output of \code{\link[=.perturb_co_split]{.perturb_co_split()}}), and tags the result with \code{draw_id}.
Analytes absent from \code{co_pert_split} (e.g. non-modelled toxicants) are left
unchanged.  The resulting rows go into the synthetic frame so that
\code{\link[=add_mspaf]{add_mspaf()}}'s normalisation sees the per-draw co-analyte perturbation.
}
\keyword{internal}
