% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mspaf.R
\name{.amspaf_adjust}
\alias{.amspaf_adjust}
\title{Normalise chemistry and apply the ARA shift for a block of sample rows}
\usage{
.amspaf_adjust(sample_rows, ref_table, ssd_params, ara_enabled = TRUE)
}
\arguments{
\item{sample_rows}{Long-format chemistry rows (one or more samples). Must
carry \code{detected}.}

\item{ref_table}{Tibble \verb{(analyte, ref_norm)}.}

\item{ssd_params}{Tibble from \code{\link[=derive_ssd_params]{derive_ssd_params()}}.}

\item{ara_enabled}{Logical; whether the caller supplied a reference.}
}
\value{
A list \code{list(tox, dropped)} where \code{tox} has the eligible,
normalisation-resolved rows (with \code{C_norm}, \code{ref_norm}, \code{ref_source},
\code{C_adj}) and \code{dropped} is a \verb{(analyte, reason)} tibble of rows dropped for
a missing required co-analyte.
}
\description{
Filters to SSD-eligible analytes, joins SSD params + the reference table,
applies the per-analyte chemistry normalisation (BDL → 0), records the
\code{ref_source} diagnostic, and computes the added-risk-adjusted concentration
\code{C_adj = max(C_norm - ref_norm, 0)}.  Operates on one \emph{or many} samples; the
caller is responsible for any per-sample grouping downstream.
}
\details{
\code{ref_source} distinguishes:
\itemize{
\item \code{"disabled"} — ARA off (no reference supplied to \code{\link[=add_amspaf]{add_amspaf()}});
\item \code{"matched"} — ARA on and a reference value was found for the analyte;
\item \code{"unmatched"} — ARA on but no reference match (assessed against raw
normalised concentration, i.e. \code{ref_norm = 0}).
}
}
\keyword{internal}
