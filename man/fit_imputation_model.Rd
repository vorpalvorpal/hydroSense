% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{fit_imputation_model}
\alias{fit_imputation_model}
\title{Fit the Bayesian multivariate imputation model(s)}
\usage{
fit_imputation_model(
  df,
  pca_vars = NULL,
  required_vars = c("pH", "EC"),
  groups = NULL,
  exclude = NULL,
  no_log_vars = NULL,
  min_detect_freq = 0.05,
  min_target_detect_freq = 0.05,
  min_samples = 10L,
  min_var_explained = 0.75,
  max_pcs = 6L,
  family = "gaussian",
  impute_method = c("rescor_mi", "cens", "cens_factor"),
  iter = 2000,
  warmup = 1000,
  chains = 4,
  cores = parallel::detectCores(),
  save_dir = NULL,
  ...
)
}
\arguments{
\item{df}{Long-format chemistry data frame with columns \code{sample_id},
\code{site_id}, \code{datetime}, \code{analyte}, \code{value}, \code{detected}.}

\item{pca_vars}{Analyte names to include in the unified chemistry PCA (used
as predictors for the brms model via PC scores).  Default: \code{c("pH", "EC", "NH3-N")} plus all \code{.WQ_BLOCK_CANDIDATES}.  Normalisation co-analytes
(DOC, Ca, Mg) are included in the default set.}

\item{required_vars}{Analyte names that must be present in a sample for it
to be retained in training and prediction.  Default \code{c("pH", "EC")}.
Samples missing any of these are dropped entirely.}

\item{groups}{A list of \code{\link[=impute_group]{impute_group()}} objects describing which analytes
to impute and how each group is hurdled.  Default \code{NULL} uses
\code{\link[=leachate_impute_groups]{leachate_impute_groups()}} (a \code{metals} group plus a catch-all \code{organics}
group).  Supply your own list to impute a different chemistry.}

\item{exclude}{Analyte names that must never be modelled as response
variables in any group (e.g. counts, qualitative descriptors).  Default
\code{NULL} uses \link{.IMPUTE_EXCLUDED}.}

\item{no_log_vars}{Analyte names that must \strong{not} be log-transformed before
the chemistry PCA (interval-scale or already-logarithmic variables such as
pH and temperature).  Default \code{NULL} uses \link{.PCA_NO_LOG_VARS}.}

\item{min_detect_freq}{Minimum detection frequency for a PCA variable to be
retained.  Default \code{0.05}.  Required vars are always retained regardless.}

\item{min_target_detect_freq}{Minimum detection frequency (fraction of
samples in which the analyte is \emph{detected}) for a metal/organic to be
included as an imputation target. Targets below this are dropped (they have
too few detections to model and would otherwise inflate the model on
near-all-BDL panels). Default \code{0.05}.}

\item{min_samples}{Minimum training samples after required-var filtering.}

\item{min_var_explained}{Target cumulative variance for PCA axis selection.
Default \code{0.75}.}

\item{max_pcs}{Maximum PCA axes to use.  Default \code{6L}.}

\item{family}{brms response family.  Must be \code{"gaussian"} (concentrations
are log-transformed before fitting; residual correlations require
Gaussian family).}

\item{impute_method}{How below-detection (BDL) values and cross-analyte
coupling are handled. One of:
\describe{
\item{\code{"rescor_mi"}}{(default) Residual correlation across analytes
(\code{rescor = TRUE}) with BDL/missing treated as imputable (\code{mi()}); the
imputed BDL cells are capped at the detection limit post-hoc by
\code{\link[=impute_chemistry]{impute_chemistry()}} (brms cannot combine \code{rescor} with \code{cens()}). Most
accurate recovery, but the \code{mi()} + correlation geometry is funnel-prone,
so \code{adapt_delta = 0.95} and an \code{lkj(2)} prior on the residual
correlation are set by default to control divergences (override via
\code{control} / \code{prior} in \code{...}).}
\item{\code{"cens"}}{Proper left-censoring of BDL at the detection limit
(\code{cens("left")}), no residual correlation -- clean BDL handling but no
cross-analyte coupling.}
\item{\code{"cens_factor"}}{Proper left-censoring \strong{with} cross-analyte
coupling. Fitted as a single long-format model with a shared per-sample
latent factor \code{(1 | sample_id)} common to all analytes, so an observed
metal informs the unobserved/BDL ones at that sample. The factor is
well-identified (each sample contributes several analyte observations);
\code{adapt_delta = 0.95} is set by default to clear the factor's mild
funnel (override via \code{control} in \code{...}).}
}
See \code{vignette("imputation")} and the package benchmark for guidance on
which to prefer.}

\item{iter, warmup, chains, cores}{brms MCMC settings.}

\item{save_dir}{If non-NULL, save the returned model object as a \code{.qs} file
in this directory using \code{qs2::qs_save()}.}

\item{...}{Additional arguments passed to \code{brms::brm()}.}
}
\value{
A named list of class \code{"imputation_model"}:
\itemize{
\item \verb{$pca}: PCA fit + metadata (loadings, training medians, n_pcs,
\code{no_log_vars}, …)
\item \verb{$groups}: a named list (one entry per fitted group, named by the group's
\code{name}); each entry has \verb{$fit} (brmsfit), \verb{$analytes}, \verb{$safe_names},
\verb{$name}, \verb{$hurdle}.  Empty if no group had any modellable analytes.
\item \verb{$group_specs}: the input list of \code{\link[=impute_group]{impute_group()}} objects
\item \verb{$required_vars}, \verb{$pca_vars}, \verb{$exclude}, \verb{$impute_method}
\item \verb{$fit_date}, \verb{$n_samples}: metadata
If \code{save_dir} is supplied, the path to the saved file is returned as
\code{attr(result, "save_path")}.
}
}
\description{
Fits a \code{brms} multivariate GAM for each \strong{imputation group} (see
\code{\link[=impute_group]{impute_group()}}), using a PCA-compressed water-quality (WQ) block as
additional environmental predictors.  The engine itself is domain-agnostic:
the leachate-specific groups (a \code{metals} group and a catch-all \code{organics}
group) are supplied by the default \code{groups = leachate_impute_groups()} and
can be swapped for any other chemistry by passing your own list of
\code{\link[=impute_group]{impute_group()}} objects.  The returned model object is passed to
\code{impute_chemistry()} for prediction on new data.
}
\details{
\strong{Model structure}

For each group, the mean structure is:

\if{html}{\out{<div class="sourceCode">}}\preformatted{s(PC1) + s(PC2) + ... + s(PCk)
}\if{html}{\out{</div>}}

where \verb{PC*} are the leading principal components of the unified chemistry
PCA (see \emph{Chemistry PCA} below).  A group's target analytes are modelled
jointly with \code{rescor = TRUE}, so observed analytes at a given sample
condition the posterior of the missing ones through the residual correlation
matrix.  Separate groups are fitted as separate models and do not share
residual correlation.

\strong{Why \code{rescor = TRUE}} — the PCA captures the \emph{instantaneous} chemical
covariance structure (what is measured together at a single moment), but
it cannot capture the \emph{temporal-lag} covariance characteristic of
AMD/leachate-impacted aquifers, where conservative tracers move ahead of
redox-controlled metals.  At a post-pulse sample the PCA scores have
returned toward baseline but Cu/Pb/Zn/Mn remain elevated together — that
co-elevation is pure residual correlation with no predictor signal driving
it.  \code{rescor = TRUE} is the right machinery for this and is what makes
multivariate imputation borrow strength across analytes.

\strong{Costs of \code{rescor = TRUE}} — brms cannot combine \code{set_rescor(TRUE)} with
\code{cens("left")}, so this implementation uses \code{mi()} for BDL values and
applies a post-hoc cap (see \code{\link[=impute_chemistry]{impute_chemistry()}}).  The cap clips imputed
BDL cells to the original detection limit when the model predicts above
DL.  For sites where the chemistry context legitimately suggests high
concentrations the cap can fire frequently; results in that regime should
be inspected.  Three alternative configurations are worth benchmarking on
real hold-out data if predictive performance becomes a concern:
\itemize{
\item \code{rescor = TRUE} + \code{mi()} (current; expected to win on plume-affected
groundwater because cross-analyte residual coupling captures plume
dynamics that the predictor PCA misses).
\item \code{rescor = FALSE} + \code{cens("left")} (statistically clean for BDL; loses
cross-analyte residual coupling).
\item \code{rescor = FALSE} + \code{cens("left")} + shared \code{(1 | sample_id)} (proper
BDL handling with rank-1 latent-factor coupling across analytes).
Benchmark methodology: mask 10\% of detected cells, fit each configuration,
compare hold-out RMSE / coverage.
}

\strong{Chemistry PCA}

All \code{pca_vars} — major ions, pH, EC, NH3-N, DOC, nutrients, redox
indicators — that are present in \code{df} and pass a detection-frequency check
are submitted to \code{nipals::nipals()}, which handles within-sample missing
cells natively without prior imputation.  Using a single unified PCA (rather
than separate driver + WQ-block sets) eliminates predictor collinearity and
ensures normalisation co-analytes (DOC, Ca, Mg) influence the imputed metal
concentrations.  Principal components are added until cumulative variance
explained reaches \code{min_var_explained} or \code{max_pcs} is reached.  A minimum
of two PCs is always used.

\strong{Hurdles (applied at prediction time by \code{impute_chemistry()})}

Each group may carry a \emph{presence hurdle} (see \code{\link[=impute_group]{impute_group()}}): a sample is
only imputed for that group if it carries at least one of the hurdle
analytes (detected or BDL).  Under the leachate preset the metals group is
hurdled on metal presence and the organics group on DOC-like presence.

\strong{BDL required variables}

When a \code{required_vars} analyte (pH or EC) is below the detection limit for a
sample, the stored detection-limit value is used as-is (conservative upper
bound).  A message is issued but the sample is retained.
}
\examples{
\dontrun{
# Requires a Stan toolchain (brms). Fit once, then reuse for imputation.
model <- fit_imputation_model(monitoring_long)
draws <- impute_chemistry(monitoring_long, model, return = "draws")

# A different domain: two custom groups instead of the leachate preset.
model2 <- fit_imputation_model(
  monitoring_long,
  groups = list(
    impute_group("trace_metals", targets = c("As", "Cd", "Pb"),
                 hurdle = c("As", "Cd", "Pb")),
    impute_group("nutrients", targets = NULL, hurdle = c("NO3-N", "P-total"))
  )
)
}
}
\seealso{
\code{\link[=impute_chemistry]{impute_chemistry()}}, \code{\link[=impute_group]{impute_group()}}, \code{\link[=leachate_impute_groups]{leachate_impute_groups()}}
}
