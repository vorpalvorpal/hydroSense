% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference_model.R
\name{.select_api_tau}
\alias{.select_api_tau}
\title{Select reservoir recession constants (tau) per analyte by profiled AIC}
\usage{
.select_api_tau(
  obs,
  hydro,
  hydro_type,
  tau_bounds_short,
  tau_bounds_long,
  default_short = 7,
  default_long = 60,
  eps = 1e-09
)
}
\arguments{
\item{obs}{Tibble \verb{(date, value_norm)} for one analyte.}

\item{hydro}{Daily hydro series.}

\item{hydro_type}{Hydro type string.}

\item{tau_bounds_short, tau_bounds_long}{Length-2 numeric \code{c(lo, hi)} search
ranges (days).  A degenerate \code{lo == hi} fixes that store's tau.}

\item{default_short, default_long}{Parsimonious fallback tau (days).}

\item{eps}{Log guard.}
}
\value{
List \verb{(tau_short, tau_long, best_aic, null_aic)}.
}
\description{
Chooses the short- and long-store recession constants \code{tau_short},
\code{tau_long} (days) for the rainfall reservoir by minimising the fitted GAM's
AIC.  Given tau the GAM is conditionally linear, so tau is selected by an
\emph{outer} 1-D optimisation of the profiled AIC — profile likelihood for a
nonlinear-in-parameter feature (Wood 2017).  The two stores are optimised by
deterministic golden-section search (\code{\link[stats:optimize]{stats::optimize()}}) in a short
coordinate descent (tau_short with tau_long held, then tau_long with
tau_short held); no RNG is used.
}
\details{
Guards:
\itemize{
\item \strong{Separation} \code{tau_long >= 1.5 * tau_short} keeps the fast and slow stores
distinct (otherwise the two smooths become collinear).
\item \strong{Overfitting gate} — the optimised tau is adopted only when it improves
AIC by \verb{>= 2} over a parsimonious default \verb{(default_short, default_long)};
otherwise the default is returned.  ΔAIC ≥ 2 is the conventional threshold
for a distinguishable model (Burnham & Anderson 2002), so a flat or
uninformative AIC surface falls back to the simpler model rather than
chasing noise.
}
}
\references{
Wood, S.N. (2017) \emph{Generalized Additive Models: An Introduction with R}, 2nd
ed. CRC Press.  Burnham, K.P. & Anderson, D.R. (2002) \emph{Model Selection and
Multimodel Inference}, 2nd ed. Springer.
}
\keyword{internal}
