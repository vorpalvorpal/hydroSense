% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kalman_bridge.R
\name{loo_anchor_coverage}
\alias{loo_anchor_coverage}
\title{Leave-one-anchor-out coverage of the daily Kalman bands}
\usage{
loo_anchor_coverage(target_model, interval = 0.9, block = 1L, scale = 1)
}
\arguments{
\item{target_model}{A fitted \code{\link[=fit_target_model]{fit_target_model()}} object (class
\code{"target_model"}).}

\item{interval}{Nominal coverage to test, in \verb{(0, 1)}. Default \code{0.9}.}

\item{block}{Number of consecutive anchors held out per fold (default \code{1}).
\code{block >= 2} probes mid-gap width rather than just the near-anchor pinch.}

\item{scale}{OU process-variance scale used while refitting, matching the
\code{ou_scale} argument of \code{\link[=mspaf_daily]{mspaf_daily()}} (a single number here). Sweep it to
find the value that brings a tier to nominal coverage.}
}
\value{
A tibble with one row per analyte (\code{analyte}, \code{tier}, \code{n} = number of
anchors, \code{coverage}, \code{mean_width}) plus a final \code{"(pooled)"} row
(anchor-weighted). \code{coverage}/\code{mean_width} are \code{NA} for analytes with too
few anchors to refit.
}
\description{
Diagnoses whether the between-grab uncertainty bands produced by
\code{\link[=mspaf_daily]{mspaf_daily()}} (\code{interpolation = "model"}) are calibrated on \strong{your} data.
For each modelled analyte it holds out each grab anchor (or a run of \code{block}
consecutive anchors), refits the OU/Kalman smoother on the rest, and checks
whether the held-out value lands inside the nominal \code{interval} predictive
band. The result is reported \strong{per analyte and per impact tier}, because the
two tiers are usually calibrated differently:
}
\details{
\itemize{
\item \strong{\code{"model"}} analytes (enough detected anchors to fit a hydrology impact
model) are normally close to nominal.
\item \strong{\code{"bridge"}} analytes (too sparse for their own fit, interpolated from a
shared shape) tend to be \strong{under-covered} — their bands are too narrow.
}

Use this to \emph{measure}, then act with \code{\link[=mspaf_daily]{mspaf_daily()}}'s tier-aware \code{ou_scale}:
if the pooled \code{bridge} coverage is below your \code{interval}, raise the bridge
tier's scale (e.g. \code{ou_scale = c(model = 1, bridge = 2)}) and re-measure. See
\code{vignette("daily-uncertainty")} for the full workflow.

Coverage is a finite-sample estimate; with few anchors (especially in the
bridge tier) it is noisy, so prefer \strong{pooling across multiple sites} before
choosing a scale.
}
\examples{
\dontrun{
# 1. Fit the daily target model (the same object mspaf_daily() builds).
tm <- fit_target_model(target_chem, reference_model = ref)

# 2. Measure calibration by tier at the default scale.
loo_anchor_coverage(tm, interval = 0.9)
#>   analyte  tier      n coverage mean_width
#>   <chr>    <chr> <int>    <dbl>      <dbl>
#>   Cu       model    38    0.895      0.71
#>   Pb       bridge   10    0.500      1.62   # under-covered
#>   (pooled) NA      194    0.85       ...

# 3. Find a bridge scale that reaches nominal, then act in the pipeline.
loo_anchor_coverage(tm, scale = 2)$coverage          # re-measure
daily <- mspaf_daily(target_chem, reference_model = ref,
                     interpolation = "model", ndraws = 200,
                     ou_scale = c(model = 1, bridge = 2))
}
}
\seealso{
\code{\link[=fit_target_model]{fit_target_model()}}, \code{\link[=mspaf_daily]{mspaf_daily()}}
}
