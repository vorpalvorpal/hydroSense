% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amspaf_daily.R
\name{.perturb_co_split}
\alias{.perturb_co_split}
\title{Draw coherent co-analyte perturbations for one draw (S7)}
\usage{
.perturb_co_split(fdm, grab_cv_co)
}
\arguments{
\item{fdm}{Fitted daily scaffold from \code{\link[=.fit_daily_target]{.fit_daily_target()}}.}

\item{grab_cv_co}{Named numeric vector of CVs per co-analyte, or a single
scalar.  \code{NULL} or \code{NA} → return \code{fdm$co_split} and \code{fdm$wq_long} unchanged.}
}
\value{
Named list \code{list(co_split, wq_long)} with perturbed values.
}
\description{
For each co-analyte grab in \code{fdm}, draws one lognormal multiplier
(geo-mean = 1, per-analyte or global CV from \code{grab_cv_co}).  Each day in
\code{co_split} inherits the multiplier of its source grab (from
\code{fdm$co_grab_map}), preserving temporal coherence: consecutive forward-
filled days that came from the same grab receive the same perturbation.
}
\keyword{internal}
