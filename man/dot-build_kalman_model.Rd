% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kalman_bridge.R
\name{.build_kalman_model}
\alias{.build_kalman_model}
\title{Build a KFAS state-space model for one residual series on a grid}
\usage{
.build_kalman_model(
  grid_dates,
  anchor_dates,
  anchor_S,
  theta,
  gamma,
  r_vec = NULL,
  q_mult = NULL
)
}
\arguments{
\item{grid_dates}{Sorted, consecutive-day Date vector: the grid to predict on.}

\item{anchor_dates, anchor_S}{Observed anchors (must fall within \code{grid_dates}).}

\item{theta, gamma}{OU parameters from \code{\link[=.estimate_ou_kalman_params]{.estimate_ou_kalman_params()}}.}

\item{r_vec}{Per-anchor observation variance (length = #anchors), or \code{NULL}
(-> a tiny value relative to gamma, so the smoother pins to the anchors).}

\item{q_mult}{Per-grid-day positive multiplier on the process variance
(hydrology modulation), length \code{length(grid_dates)}, or \code{NULL} (-> 1).}
}
\value{
A KFAS \code{SSModel}, or \code{NULL} if \code{gamma <= 0} or no anchors on the grid.
The grid is stored in \code{attr(., "grid_dates")}.
}
\description{
\strong{Invariant:} \code{grid_dates} must be \emph{consecutive days} (step = 1 day). The
transition \code{phi = exp(-theta)} assumes a unit time step between adjacent grid
points, so a sparse/irregular grid would mis-state the correlation length.
}
\keyword{internal}
