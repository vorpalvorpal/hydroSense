% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference.R
\name{.normalise_ref_observations}
\alias{.normalise_ref_observations}
\title{Normalise reference observations to the SSD index condition}
\usage{
.normalise_ref_observations(ref_df, original_data, meta)
}
\arguments{
\item{ref_df}{Long-format chemistry data frame with at minimum \code{analyte},
\code{value}, \code{detected}, and \code{sample_id}.}

\item{original_data}{The original reference chemistry frame used to look up
co-analyte values (pH, DOC, hardness, Ca, Mg) per sample.}

\item{meta}{Analyte metadata tibble from \code{.load_analyte_metadata()}.}
}
\value{
\code{ref_df} with a \code{value_norm} numeric column appended (NA where
normalisation fails due to a missing required co-analyte).
}
\description{
Applies per-analyte bioavailability / physicochemical normalisation formulas
(hardness, pH, DOC) to a long-format chemistry data frame, returning the
frame with a \code{value_norm} column added.  BDL rows (\code{detected == FALSE}) are
normalised at their detection-limit value but callers are responsible for
setting \code{value = 0} for BDL rows \emph{before} calling this if they want BDL to
contribute zero to downstream summaries (as \code{prepare_reference()} does).
}
\keyword{internal}
