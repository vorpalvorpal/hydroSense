% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/route-c.R
\name{rhat.route_c_stanfit}
\alias{rhat.route_c_stanfit}
\title{Rhat for a Route C Stage-2 Stan fit}
\usage{
\method{rhat}{route_c_stanfit}(x, ...)
}
\arguments{
\item{x}{A \code{route_c_stanfit} object (a \code{CmdStanMCMC} with this class prepended).}

\item{...}{Unused; present for S3 signature compatibility.}
}
\value{
Named numeric vector of per-parameter Rhat.
}
\description{
\code{.fit_group_model_factor()} tags its Stage-2 \code{CmdStanMCMC} fit with class
\code{route_c_stanfit} so generic diagnostics like \code{brms::rhat()} (itself just
\code{posterior::rhat()}, a plain S3 generic) work on it directly. The raw
cmdstanr object has no \code{rhat()} method of its own to dispatch to; this
delegates to its \verb{$summary()} method, which already computes per-parameter
Rhat correctly (unlike calling \code{posterior::rhat()} on the whole multi-
parameter draws array, which silently blends every parameter into one
meaningless number).
}
