% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/route-c.R
\name{.predict_factor_conditional}
\alias{.predict_factor_conditional}
\title{Closed-form conditional prediction for the Route C factor model}
\usage{
.predict_factor_conditional(
  group,
  pc_wide,
  df_eligible,
  return,
  ndraws,
  batch_size
)
}
\arguments{
\item{group}{A fitted \code{factor}-method group.}

\item{pc_wide}{Per-sample PC scores for the eligible samples (\code{sample_id}
\itemize{
\item \verb{PC*} columns).
}}

\item{df_eligible}{The long-format input rows for the eligible samples
(supplies each sample's own observed target values).}
}
\description{
For each eligible sample, builds the residual vector \code{y} (observed minus
the Stage-1 GAM mean for detected target cells; \code{NA} for BDL/missing
cells), then applies \code{.factor_condition()} / \code{.factor_condition_draw()}
per posterior draw of \verb{(Lambda, Psi)} to predict the missing cells —
conditioned on this sample's \emph{own} observed analytes (finding 3), with any
BDL target truncated at \code{log(DL)} (findings 1-2). Mirrors
\code{.predict_factor_long()}'s \code{pm_long} return shape so
\code{.predict_and_merge()}'s merge/fabricate logic is unchanged; only rows for
cells that actually need prediction (BDL or entirely absent) are emitted.
}
\keyword{internal}
