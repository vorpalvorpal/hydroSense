% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ou_bridge.R
\name{.ou_bridge_draw}
\alias{.ou_bridge_draw}
\title{Draw one coherent mean-zero OU-bridge fluctuation path}
\usage{
.ou_bridge_draw(factors)
}
\arguments{
\item{factors}{Output of \code{\link[=.ou_bridge_factors]{.ou_bridge_factors()}}.}
}
\value{
Numeric vector of length \code{factors$n_target}: ε for each target
date in the sorted order used when calling \code{\link[=.ou_bridge_factors]{.ou_bridge_factors()}}.
Call \code{\link[=set.seed]{set.seed()}} before this function for reproducibility.
}
\description{
Uses precomputed Cholesky factors from \code{\link[=.ou_bridge_factors]{.ou_bridge_factors()}} to generate
one realisation of the latent residual-state fluctuation ε(t) over the
full daily grid.
}
\details{
All anchor dates carry ε = 0 (pinch condition).  Within each inter-anchor
gap, interior days are drawn jointly from the conditional Gaussian
distribution via the precomputed lower-triangular Cholesky \code{L}:
\verb{ε_gap = L \%*\% rnorm(m)}.  This is O(m) per draw (the O(m³) Cholesky
was paid once in \code{\link[=.ou_bridge_factors]{.ou_bridge_factors()}}).

For leading/trailing extrapolation zones, each day is drawn independently
from \verb{N(0, SD²)} where SD grows from 0 at the anchor toward √γ.
}
\keyword{internal}
