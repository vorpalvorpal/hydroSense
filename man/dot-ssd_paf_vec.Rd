% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mspaf.R
\name{.ssd_paf_vec}
\alias{.ssd_paf_vec}
\title{Vectorised SSD PAF lookup for one analyte}
\usage{
.ssd_paf_vec(fit, conc, analyte, method, guideline_dir)
}
\arguments{
\item{fit}{Fitted SSD object (from the \code{fit} list-column) or \code{NULL}.}

\item{conc}{Numeric vector of adjusted concentrations.}

\item{analyte}{Analyte name (fallback \code{ssd_paf()} lookup only).}

\item{method}{SSD method (fallback only).}

\item{guideline_dir}{Path to ANZG XLSX folder (fallback only).}
}
\value{
Numeric vector the same length as \code{conc}, PAF as a proportion (0–1).
}
\description{
Returns the proportion of species affected at each concentration in \code{conc}.
Concentrations that are \code{NA} or \verb{<= 0} map to \code{PAF = 0}.  When a fitted SSD
object is available it is evaluated in a single \code{\link[ssdtools:ssd_hp]{ssdtools::ssd_hp()}} call
over the positive concentrations; otherwise it falls back to a per-value
\code{\link[=ssd_paf]{ssd_paf()}} lookup (which re-resolves the model internally).
}
\keyword{internal}
