% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kalman_bridge.R
\name{.kalman_sim_smoother_setup}
\alias{.kalman_sim_smoother_setup}
\title{Precompute the DK simulation smoother gain matrix for a KFAS model}
\usage{
.kalman_sim_smoother_setup(model)
}
\arguments{
\item{model}{A KFAS \code{SSModel} from \code{\link[=.build_kalman_model]{.build_kalman_model()}}.}
}
\value{
A list with components:
\describe{
\item{\code{L}}{\verb{[n_grid × m]} gain matrix (standardised units).}
\item{\code{pos}}{Integer indices of anchor positions in the grid.}
\item{\code{x_hat}}{KFS posterior mean in un-standardised units (length n_grid).}
\item{\code{phi}}{Scalar AR(1) coefficient.}
\item{\code{q_sd_vec}}{Standardised process noise SD per grid day (length n_grid).}
\item{\code{h_sd_vec}}{Standardised observation noise SD at anchor positions (length m).}
\item{\code{resid_scale}}{Un-standardising factor (\code{sqrt(gamma)}).}
\item{\code{n_grid}}{Number of grid days.}
}
}
\description{
Builds the gain matrix \code{L} (n_grid × m, standardised units) such that
\code{L \%*\% y_anch} recovers the KFS-smoothed state when \code{y_anch} is placed at
the anchor positions. This relies on the linearity of the KFS smoother in
\code{y} when \code{a1 = 0}: the smoother output is a linear function of the
observations, so it can be precomputed column-by-column by running KFS on
unit vectors (Durbin & Koopman 2002, Biometrika 89(3), §4).
}
\details{
The returned \code{x_hat} is in \strong{original (un-standardised) units} to match
the output of \code{\link[=.kalman_smooth]{.kalman_smooth()}}.
}
\references{
Durbin J, Koopman SJ (2002). A simple and efficient simulation
smoother for state space time series analysis. \emph{Biometrika} \strong{89}(3),
603–616. \url{https://doi.org/10.1093/biomet/89.3.603}
}
\keyword{internal}
