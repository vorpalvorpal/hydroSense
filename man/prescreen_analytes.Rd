% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prescreen.R
\name{prescreen_analytes}
\alias{prescreen_analytes}
\title{Pre-screen analytes by detection frequency}
\usage{
prescreen_analytes(
  df,
  k = 0.05,
  protect = NULL,
  potency_keep = TRUE,
  potency_frac = 1,
  group_by_feature = FALSE,
  conc_units = NULL,
  analyte_metadata = NULL,
  return = c("vector", "table")
)
}
\arguments{
\item{df}{Long-format chemistry data frame.  Required columns:
\code{sample_id} (character), \code{analyte} (character), \code{detected} (logical).
When \code{group_by_feature = TRUE}, also requires \code{site_id} (character).}

\item{k}{Minimum detection frequency (proportion, 0–1). Analytes with
\code{n_detected / n_samples < k} are excluded unless protected. Default
\code{0.05} (5 \%).}

\item{protect}{Optional character vector of additional analyte names to
protect from prescreen exclusion, on top of the metadata-derived
co-analytes.  Default \code{NULL}.  Pass the \code{required_vars} from your
downstream imputation step here.}

\item{potency_keep}{Logical. Enable the potency escape hatch (keep a
frequency-failing analyte whose concentration reaches its guideline value).
Default \code{TRUE}. Set \code{FALSE} for a frequency-only prescreen.}

\item{potency_frac}{Numeric (>= 0). Fraction of the 95 \% guideline value
(\code{dgv_95pct_ug_L}) a detected concentration must reach to rescue an
analyte. Default \code{1} (must reach the guideline). Lower it (e.g. \code{0.5}) to
be more precautionary.}

\item{group_by_feature}{Logical. If \code{TRUE}, detection frequency is computed
per \code{site_id} and an analyte is included only if it passes in \emph{all}
features (worst-case feature, pooled counts). If \code{FALSE} (default),
frequency is pooled across all samples.}

\item{conc_units}{Character. Unit string (e.g. \code{"mg/L"}, \code{"ug/L"}) applied
to toxicant concentrations in \code{df} when it has no \code{units.analyte} column.
Only matters when \code{potency_keep = TRUE}, because the potency threshold
comparison needs values in µg/L (matching \code{dgv_95pct_ug_L}). Ignored when
\code{potency_keep = FALSE} or \code{df} already carries a \code{units.analyte} column.}

\item{analyte_metadata}{Data frame of analyte metadata, or \code{NULL} to load
the bundled \code{inst/extdata/anzecc_analyte_metadata.csv}. Used to identify
co-analytes that must be protected from exclusion.}

\item{return}{Either \code{"vector"} (default) to return a character vector of
included analyte names, or \code{"table"} to return a tibble with one row per
analyte showing detection statistics and inclusion flag.}
}
\value{
When \code{return = "vector"}: a character vector of passing analyte
names. The vector carries an attribute \code{"excluded"} listing analytes that
did not pass the threshold (non-protected only), so callers can record
what was dropped.

When \code{return = "table"}: a tibble with columns \code{analyte},
\code{n_samples}, \code{n_detected}, \code{detect_freq}, \code{limiting_site},
\code{protected} (logical), \code{potency_kept} (logical; rescued by the potency
escape hatch), \code{included} (logical).  When
\code{group_by_feature = TRUE}, \code{n_samples} / \code{n_detected} / \code{detect_freq}
describe the \emph{limiting} site (the worst-case feature that drove the
inclusion decision) and \code{limiting_site} names it; when
\code{group_by_feature = FALSE}, counts are pooled across all samples and
\code{limiting_site} is \code{NA}.
}
\description{
Computes detection frequency (proportion of samples with \code{detected == TRUE})
per analyte and returns the names that meet a minimum threshold. Use this
before \code{impute_chemistry()} to drop analytes that were almost never detected —
imputing such analytes from near-zero priors adds noise without ecological
signal.
}
\details{
Analytes listed in \code{coanalytes_required} in the bundled metadata (e.g. pH,
DOC, Ca, Mg, hardness, temperature) are \strong{automatically protected} from
exclusion regardless of detection frequency, because they are needed for
chemistry normalisation in \code{\link[=add_mspaf]{add_mspaf()}}.  Additional analytes can be
protected via the \code{protect} argument (typical use: pass the
\code{required_vars} you intend to use in \code{\link[=fit_imputation_model]{fit_imputation_model()}} here so
those vars survive prescreen).  Protected analytes that fall below the
threshold are reported separately so the caller is aware.

\strong{Potency escape hatch.} Frequency alone can screen out a rare-but-potent
toxicant (e.g. a pesticide detected in 2 \% of samples but at
ecotoxicologically significant concentrations). With \code{potency_keep = TRUE}
(default), an analyte that fails the frequency threshold is still kept if any
detected concentration reaches \code{potency_frac} times its 95 \%-species-
protection guideline value (\code{dgv_95pct_ug_L} in the metadata). This needs a
numeric \code{value} column in the same units as the guideline (µg/L); only
analytes that carry a guideline value (toxicants) can be rescued this way, so
major ions and analytes with no guideline are unaffected.
}
\examples{
included <- prescreen_analytes(leachate_demo(), k = 0.05)
included                    # analytes retained
attr(included, "excluded")  # see what was dropped (and why)

}
