% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{leachate_demo}
\alias{leachate_demo}
\title{Synthetic leachate-impacted water-quality data (demo)}
\format{
A \link[tibble:tibble]{tibble} with one row per sample x analyte and the
columns:
\describe{
\item{sample_id}{Character. Unique sample identifier.}
\item{site_id}{Character. One of \code{"downstream"}, \code{"reference"},
\code{"leachate"}.}
\item{datetime}{Date. Sampling date.}
\item{analyte}{Character. Analyte name.}
\item{value}{Numeric. Concentration in \code{units.analyte}.}
\item{detected}{Logical. \code{FALSE} marks a below-detection-limit result.}
\item{units.analyte}{Character. Concentration units: major ions in
\code{"mg/L"}; \code{Cu}/\code{Zn}/\code{NH3-N} in \code{"ug/L"} (the SSD scale); \code{pH} unitless;
\code{temperature} in \code{"degC"}; \code{hardness} in \code{"mg/L CaCO3"}.}
\item{valence.analyte}{Numeric. Ionic charge (for the meq conversion in
\code{\link[=to_meq]{to_meq()}}); \code{NA} for non-ionic analytes.}
\item{atomic_mass.analyte}{Numeric. Molar/atomic mass in g/mol (for the
meq conversion); \code{NA} for non-ionic analytes.}
}
}
\usage{
leachate_demo
}
\description{
A small, fictional long-format water-quality dataset used in examples and
the package vignettes. It is \strong{not} real monitoring data — concentrations
are generated from a simple two-component mixing model so the worked
examples are reproducible and self-contained.
}
\details{
Three sites are included:
\describe{
\item{\code{downstream}}{A leachate-impacted site, ~15\% leachate mixing
fraction relative to the reference.}
\item{\code{reference}}{Clean background / upstream chemistry.}
\item{\code{leachate}}{The leachate end-member.}
}

Each site has six bi-monthly samples through 2024. The analyte panel carries
the major ions used by \code{\link[=add_lmf]{add_lmf()}} / \code{\link[=to_meq]{to_meq()}} (\code{Na}, \code{K}, \code{Ca}, \code{Mg},
\code{Cl}, \code{SO4}-with-charge, \code{F}, \code{NO3-N}, \code{NO2-N}, \code{CO3-CaCO3}, \code{HCO3-CaCO3}),
the toxicants assessed by \code{\link[=ssd_paf]{ssd_paf()}} / \code{\link[=add_amspaf]{add_amspaf()}} (\code{Cu}, \code{Zn},
\code{NH3-N}), and the co-analytes needed for the ammonia and bioavailability
normalisations (\code{pH}, \code{temperature}, \code{DOC}, \code{hardness}).
}
\examples{
# Leachate-mixing fraction per downstream sample:
\donttest{
add_lmf(
  df             = subset(leachate_demo, site_id == "downstream"),
  leachate_data  = subset(leachate_demo, site_id == "leachate"),
  reference_data = subset(leachate_demo, site_id == "reference")
)
}
}
\keyword{datasets}
