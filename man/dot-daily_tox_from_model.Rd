% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amspaf_daily.R
\name{.daily_tox_from_model}
\alias{.daily_tox_from_model}
\title{Model-based daily toxicant interpolation (season-blind impact model)}
\usage{
.daily_tox_from_model(
  daily_long,
  site_rows,
  reference_model,
  imputation_model,
  conc_units,
  meta,
  tox_analytes
)
}
\arguments{
\item{daily_long}{Output of \code{.build_daily_chem()} (+ temperature fill).}

\item{site_rows}{Grab chemistry for the site (passed to the target model).}

\item{reference_model}{A \code{reference_model}.}

\item{imputation_model}{Optional \code{imputation_model} (tier-2 enrichment).}

\item{conc_units}{Units for \code{site_rows} toxicants when no \code{units.analyte}.}

\item{meta}{Analyte metadata (normalisation formulas).}

\item{tox_analytes}{SSD-eligible analyte names.}
}
\value{
\code{daily_long} with modelled-toxicant rows replaced.
}
\description{
Fits a \code{\link[=fit_target_model]{fit_target_model()}} on the site's grab chemistry and the supplied
\code{reference_model}, predicts each toxicant's normalised concentration
\code{C_norm = ref_norm + impact} on every daily date, and reconstructs the raw
µg/L concentration \code{C_raw = C_norm / factor} (the normalisation is
multiplicative, so the factor is \code{normalise(1, co-analytes_of_the_day)}).
Modelled-toxicant rows in \code{daily_long} are replaced with these estimates
(in µg/L, \code{units.analyte = "ug/L"}); co-analytes and non-modelled toxicants
are left untouched. On any failure the input is returned unchanged.
}
\keyword{internal}
