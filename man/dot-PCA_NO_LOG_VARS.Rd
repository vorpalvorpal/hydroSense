% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\docType{data}
\name{.PCA_NO_LOG_VARS}
\alias{.PCA_NO_LOG_VARS}
\title{PCA variables that must NOT be log-transformed before the chemistry PCA}
\format{
An object of class \code{character} of length 4.
}
\usage{
.PCA_NO_LOG_VARS
}
\description{
Every other PCA variable is concentration-like — strictly positive and
strongly right-skewed, spanning orders of magnitude — so it is
\code{log10}-transformed before centring/scaling.  Without that, the PCA is
dominated by a handful of high-magnitude major ions (e.g. Cl, SO4, TDS) and
the leading axes mostly track absolute ionic strength rather than the
multiplicative covariance structure that drives metal/organic behaviour.
}
\details{
The exclusions are the variables for which a log is meaningless or undefined:
\itemize{
\item \code{pH} — already a logarithmic scale (−log10 of H+ activity).
\item \code{temperature} — interval scale (°C); zero/negative values are valid.
\item \code{ORP} — redox potential (mV); routinely negative.
\item \code{DO} — dissolved oxygen (mg/L); legitimately ~0 in anoxic leachate
plumes and only spans a narrow, near-linear range.
}
}
\keyword{internal}
