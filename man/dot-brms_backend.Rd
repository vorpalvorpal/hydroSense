% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{.brms_backend}
\alias{.brms_backend}
\title{Choose the brms/Stan backend for imputation fits}
\usage{
.brms_backend()
}
\description{
Prefers \strong{cmdstanr} when both the package and a working CmdStan install are
present, otherwise falls back to \strong{rstan}.  cmdstanr caches compiled model
binaries (a warm refit skips compilation: ~3x faster in the package
benchmark) and samples faster, while being statistically equivalent to rstan
(posterior-mean correlation 0.99999, max standardized difference ~0.12 SD on
the imputation model; see \code{dev/bench-backend.R}).  Override with
\code{options(hydroSense.brms_backend = "rstan")} (or \code{"cmdstanr"}).  An explicit
\code{backend} passed through \code{...} to \code{\link[=fit_imputation_model]{fit_imputation_model()}} always wins.
}
\keyword{internal}
