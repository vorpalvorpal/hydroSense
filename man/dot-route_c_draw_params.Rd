% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/route-c.R
\name{.route_c_draw_params}
\alias{.route_c_draw_params}
\title{Extract per-draw (Lambda, Psi) pairs from a fitted Route C group}
\usage{
.route_c_draw_params(group, ndraws = NULL)
}
\arguments{
\item{group}{A fitted \code{factor}-method group (from \code{.fit_group_model_factor()}).}

\item{ndraws}{Optional cap on the number of posterior draws to use (the
first \code{ndraws}); \code{NULL} uses every draw.}
}
\value{
\verb{list(Lambda = list of J x k matrices, Psi = list of length-J vectors)}, one entry per used draw, plus \code{n} (the number used).
}
\description{
Extract per-draw (Lambda, Psi) pairs from a fitted Route C group
}
\keyword{internal}
