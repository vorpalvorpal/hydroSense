% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amspaf_daily.R
\name{.perturb_anchors_in_model}
\alias{.perturb_anchors_in_model}
\title{Perturb anchor residual-state values by grab measurement error (S6)}
\usage{
.perturb_anchors_in_model(tm, fdm, grab_cv)
}
\arguments{
\item{tm}{A \code{target_model} (typically already GAM-perturbed).}

\item{fdm}{Fitted daily scaffold from \code{\link[=.fit_daily_target]{.fit_daily_target()}}; provides
\code{c_norm_obs_anch} and \code{has_wq} flags.}

\item{grab_cv}{Named numeric vector of CVs per analyte, or a single scalar
applied to all analytes.  Analytes not present in the vector (and without
a scalar default) are left unperturbed.}
}
\value{
A modified copy of \code{tm} with perturbed anchor S values.
}
\description{
For each modelled analyte, draws one lognormal multiplier per anchor grab
(geo-mean = 1, CV = \code{grab_cv[[nm]]}) and shifts the anchor's S value by
\verb{ΔS = C_norm_obs × (mult − 1)}, where \code{C_norm_obs} was precomputed in
\code{\link[=.fit_daily_target]{.fit_daily_target()}}.  WQ-tier analytes' \code{d_anchors} are treated the same
way (their residual \code{d} responds to C_norm perturbation identically).
}
\details{
The updated \code{tm} copy is then passed to \code{\link[=.predict_daily_tox]{.predict_daily_tox()}} for one
draw; the \code{.interp_residual()} bridge will interpolate between the
perturbed anchor values, providing at-anchor spread equal to the grab
measurement width and mid-gap spread equal to that plus the OU balloon.
}
\keyword{internal}
