% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{leachate_impute_groups}
\alias{leachate_impute_groups}
\title{Leachate imputation-group preset}
\usage{
leachate_impute_groups()
}
\value{
A list of two \code{"impute_group"} objects (\code{metals}, \code{organics}).
}
\description{
Returns the default \code{\link[=impute_group]{impute_group()}} specification used by
\code{\link[=fit_imputation_model]{fit_imputation_model()}} for landfill-leachate monitoring chemistry: a
\strong{metals} group (hurdled on metal presence) and a catch-all \strong{organics}
group (hurdled on dissolved-organic-carbon presence).  This is the leachate
domain layer on top of the otherwise domain-agnostic engine — pass your own
list of \code{\link[=impute_group]{impute_group()}} objects to model a different chemistry.
}
\details{
The metals set is \link{.METAL_ANALYTES} and the organic-carbon hurdle set is
\link{.DOC_LIKE_ANALYTES}. The metals presence hurdle excludes the redox
indicators Fe and Mn (routine analytes kept as PCA predictors, not trace-
metal contamination signals), so a sample reporting only Fe/Mn does not
trigger fabrication of the other trace metals.
}
\examples{
leachate_impute_groups()
}
\seealso{
\code{\link[=impute_group]{impute_group()}}, \code{\link[=fit_imputation_model]{fit_imputation_model()}}
}
