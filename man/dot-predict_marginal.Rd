% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/route-c.R
\name{.predict_marginal}
\alias{.predict_marginal}
\title{Posterior-predictive prediction for the marginal method}
\usage{
.predict_marginal(group, pc_wide, df_eligible, return, ndraws, batch_size)
}
\description{
Per analyte, per cell that needs imputing (BDL or entirely absent): draw the
GAM mean with parameter uncertainty (\code{beta ~ N(coef, Vp)}, \code{Vp} unconditional
so smoothing-parameter uncertainty is included) plus residual noise
\code{N(0, sig2)}; BDL cells draw the residual left-truncated so the value never
exceeds \code{log(DL)}. This is a proper posterior predictive (mean + residual
uncertainty), not the plug-in residual-only draw. Returns the same \code{pm_long}
shape as the other predictors so \code{.predict_and_merge()} is unchanged.
}
\keyword{internal}
