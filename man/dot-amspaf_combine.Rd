% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mspaf.R
\name{.amspaf_combine}
\alias{.amspaf_combine}
\title{Combine adjusted+PAF'd tox rows for one sample into a single AmsPAF row}
\usage{
.amspaf_combine(tox, has_imputed = FALSE)
}
\arguments{
\item{tox}{Rows for one sample from \code{\link[=.amspaf_add_paf]{.amspaf_add_paf()}} (needs \code{C_adj},
\code{hc50}, \code{sigma}, \code{moa_group}, \code{PAF}; \code{analyte}, \code{ref_source} carried into
the \code{analyte_pafs} diagnostic if present).}

\item{has_imputed}{Logical; whether the input carried an \code{imputed} column.}
}
\value{
A one-row tibble: \code{value}, \code{n_analytes_used}, \code{n_analytes_imputed},
\code{dominant_analyte}, \code{max_paf}, \code{analyte_pafs} (does NOT add \code{sample_id} or
\code{dropped_analytes} — the caller attaches those).
}
\description{
Performs Concentration Addition within each mode-of-action group and
Independent Action across groups, then assembles the one-row diagnostic
tibble.  Operates on a \emph{single} sample's rows.
}
\keyword{internal}
