% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/route-c.R
\name{.fit_group_model_factor_degenerate}
\alias{.fit_group_model_factor_degenerate}
\title{Stage-1-only fit for a single-analyte factor group (no cross-analyte
coupling is possible with J = 1)}
\usage{
.fit_group_model_factor_degenerate(
  target_analytes,
  safe_analytes,
  base,
  pc_wide,
  pc_cols,
  log_floors,
  group_name = "group"
)
}
\description{
Fits the Stage-1 GAM as usual and estimates a plug-in residual variance
from the detected residuals. No Stan model is fit; \code{Lambda} is a J x 0
matrix (\code{k = 0}) so \code{.factor_condition()}'s existing \code{length(obs) == 0}
branch (always true here -- a single-analyte group's own target is either
fully observed, in which case there is nothing to predict, or missing, in
which case there is nothing else in the group to condition on) reduces
exactly to the marginal \code{N(mu_j, Psi_j)}, letting \code{.predict_factor_conditional()}
and \code{.route_c_draw_params()} handle this group with no special-casing.
}
\keyword{internal}
