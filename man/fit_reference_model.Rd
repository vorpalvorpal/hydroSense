% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference_model.R
\name{fit_reference_model}
\alias{fit_reference_model}
\title{Fit a temporal reference model for contemporaneous ARA background subtraction}
\usage{
fit_reference_model(
  reference,
  hydro = NULL,
  hydro_type = "rainfall",
  latitude = NULL,
  longitude = NULL,
  conc_units = NULL,
  analyte_metadata = NULL,
  imputation_model = NULL,
  match_window_days = 5L,
  match_hydro_tol = NULL,
  api_tau_bounds_short = c(1, 30),
  api_tau_bounds_long = c(20, 365),
  auto_select = TRUE,
  min_obs_model = 20L,
  summary = "geom_mean",
  silo_start = NULL,
  silo_end = NULL,
  silo_api_key = NULL,
  eps = 1e-09
)
}
\arguments{
\item{reference}{Long-format reference chemistry data frame.  Required
columns: \code{sample_id}, \code{site_id}, \code{datetime}, \code{analyte}, \code{value},
\code{detected}.  Toxicant concentrations must be in µg/L; supply them via a
\code{units.analyte} column or via \code{conc_units}.}

\item{hydro}{Daily hydrology data frame (\code{date}, \code{value}), or \code{NULL} to
fetch SILO rainfall automatically.  See \emph{Hydrology input} above.}

\item{hydro_type}{Character; one of \code{"rainfall"}, \code{"stage"}, \code{"discharge"}.
Ignored if \code{hydro} already has a \code{type} column.  Default \code{"rainfall"}.}

\item{latitude, longitude}{Decimal-degree coordinates; required only when
\code{hydro = NULL} (SILO auto-fetch path).}

\item{conc_units}{Unit string (e.g. \code{"mg/L"}, \code{"ug/L"}) for reference
chemistry when no \code{units.analyte} column is present.}

\item{analyte_metadata}{Analyte metadata, or \code{NULL} for the bundled CSV.}

\item{imputation_model}{Optional \code{imputation_model} from
\code{\link[=fit_imputation_model]{fit_imputation_model()}} (fit on the reference site's own chemistry).  When
supplied, missing analytes are imputed in raw concentration space \emph{before}
the per-analyte models are fitted, so a well-sampled analyte lifts a
sparsely-sampled one into a richer spread of hydrological regimes.  Imputed
rows (\code{detected = TRUE}) are used as model anchors alongside measured
observations.  Requires \strong{brms}.  Default \code{NULL} (measured observations
only).}

\item{match_window_days}{Integer; tier-1 time tolerance in days (default 5).}

\item{match_hydro_tol}{Numeric; tier-1 hydro tolerance (default \code{NULL} →
\verb{0.5 × IQR} of the reference event-API series).}

\item{api_tau_bounds_short}{Length-2 numeric \code{c(lo, hi)} search range (days)
for the short-store recession constant \code{tau_short} (default \code{c(1, 30)}).
A degenerate \code{c(x, x)} fixes \code{tau_short = x}.}

\item{api_tau_bounds_long}{Length-2 numeric \code{c(lo, hi)} search range (days)
for the long-store recession constant \code{tau_long} (default \code{c(20, 365)}).}

\item{auto_select}{Logical; if \code{TRUE} (default), select \code{tau_short},
\code{tau_long} per analyte by profiled AIC.  If \code{FALSE}, use the parsimonious
defaults (\code{tau_short = 7}, \code{tau_long = 60}) for all analytes.}

\item{min_obs_model}{Integer; minimum detected observations required to
attempt tier-2 modelling (default \code{20L}).  Analytes below this fall
directly to tier 3.}

\item{summary}{Static-fallback summary statistic (tier 3): one of
\code{"geom_mean"} (default), \code{"median"}, \code{"arith_mean"}, \code{"p80"}, \code{"p90"},
\code{"p95"}.}

\item{silo_start, silo_end}{Start/end dates for SILO auto-fetch.  Default
\code{NULL}: derived from the reference chemistry date range, padded on the left
by \verb{5 × max(api_tau_bounds_long)} days so the recursive reservoir has
enough burn-in (about 5 tau) to converge before the first observation.}

\item{silo_api_key}{Passed to \code{\link[=get_silo_rainfall]{get_silo_rainfall()}} when \code{hydro = NULL}.}

\item{eps}{Small positive guard for log transform (default \code{1e-9}).}
}
\value{
An object of class \code{reference_model}:
\describe{
\item{\verb{$models}}{Named list (one per analyte) carrying \code{gamm_fit},
\code{tau_short}, \code{tau_long}, \code{best_aic}, \code{null_aic}, \code{tier},
\code{n_obs}, \code{static_ref}, and \code{obs} (normalised observations with
hydro features — used for tier-1 matching).}
\item{\verb{$hydro}}{Daily hydro series used for index computation.}
\item{\verb{$hydro_type}}{\code{"rainfall"}, \code{"stage"}, or \code{"discharge"}.}
\item{\verb{$match_window_days}}{Tier-1 time window.}
\item{\verb{$match_hydro_tol}}{Tier-1 hydro tolerance (computed or supplied).}
\item{\verb{$static_ref}}{Tibble \verb{(analyte, ref_norm)} — tier-3 fallback for
all analytes.}
\item{\verb{$fit_date}}{Date the model was fitted.}
}
}
\description{
Fits per-analyte temporal models on reference-site chemistry so that
\code{\link[=add_amspaf]{add_amspaf()}} can subtract a \emph{contemporaneous} background (what the
reference site would have shown at the same moment as the target sample)
rather than a static time-average.
}
\details{
\strong{Package invariant:} reference and target are assumed to share the same
catchment.  The hydrology series supplied here (or fetched automatically
from SILO) applies to both sites.  This invariant should hold for
near-field leachate assessments where a headwater reference is paired with
a downstream target in the same sub-catchment.

\strong{Three-tier resolver} (evaluated per analyte × target date):
\enumerate{
\item \strong{Tier 1 — direct match.} A reference observation within
\verb{±match_window_days} days whose event-API is within \code{match_hydro_tol}
(default: \verb{0.5 × IQR} of the reference API series).  This gate rejects
time-close but hydrologically-mismatched grabs (e.g. a dry-weather
reference next to a wet-event target).
\item \strong{Tier 2 — GAM prediction.} A per-analyte \code{mgcv::gam} with
\code{s(doy, bs="cc") + s(hydro_short) + s(hydro_long)}.  The reservoir
recession constants \code{tau_short}, \code{tau_long} are selected per analyte by
profiled AIC over the \code{api_tau_bounds_short}/\code{api_tau_bounds_long}
ranges (continuous, with a \code{tau_long >= 1.5*tau_short} separation and a
ΔAIC ≥ 2 adoption gate over a parsimonious default).  The analyte falls
back to tier 3 if the best model has higher AIC than the null
(intercept-only) model, or if fewer than \code{min_obs_model} detected
observations are available.
\item \strong{Tier 3 — static fallback.} Geometric mean of all normalised
reference observations (identical to \code{\link[=prepare_reference]{prepare_reference()}} with
\code{summary = "geom_mean"}).
}

\strong{Hydrology input} — supply exactly one of:
\itemize{
\item \code{hydro}: a data frame with columns \code{date} (Date) and \code{value}
(numeric), plus a \code{type} column OR supply \verb{hydro_type =} separately.
Supported types: \code{"rainfall"} (daily mm; → API), \code{"stage"} (m;
→ antecedent mean), \code{"discharge"} (m³/s; → antecedent mean).
\item \code{latitude} + \code{longitude}: if \code{hydro = NULL}, SILO daily rainfall is
fetched automatically via \code{\link[=get_silo_rainfall]{get_silo_rainfall()}}.  Requires the
\strong{weatherOz} package and a valid SILO API key.
}
}
\examples{
\dontrun{
# Fit using SILO auto-fetch
ref <- subset(leachate_demo(), site_id == "reference")
ref_model <- fit_reference_model(
  reference  = ref,
  latitude   = -33.87,
  longitude  = 151.21,
  conc_units = "ug/L"
)
ref_model

# Or supply your own gauge record
ref_model2 <- fit_reference_model(
  reference  = ref,
  hydro      = my_stage_df, # data.frame(date, value)
  hydro_type = "stage",
  conc_units = "ug/L"
)
}
}
\seealso{
\code{\link[=add_amspaf]{add_amspaf()}}, \code{\link[=get_silo_rainfall]{get_silo_rainfall()}}, \code{\link[=prepare_reference]{prepare_reference()}},
\code{\link[=ara_summary]{ara_summary()}}
}
