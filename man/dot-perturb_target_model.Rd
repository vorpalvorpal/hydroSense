% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draws.R
\name{.perturb_target_model}
\alias{.perturb_target_model}
\title{Perturb all GAM fits inside a target_model for one draw}
\usage{
.perturb_target_model(tm, perturb_reference = FALSE)
}
\arguments{
\item{tm}{A \code{target_model} from \code{\link[=fit_target_model]{fit_target_model()}}.}

\item{perturb_reference}{Logical; when \code{TRUE}, also perturb the embedded
\code{reference_model}'s GAMs (use only when \code{mspaf_daily(reference = NULL)}
— total-concentration mode — so that \code{ref_norm} uncertainty is not
inadvertently cancelled by a downstream subtraction).}
}
\value{
A modified copy of \code{tm}.  The original object is not mutated
(R copy-on-modify semantics).
}
\description{
Draws posterior-coefficient samples for the impact GAMs and WQ-layer GAMs.
Pooled analytes (all sharing one factor-smooth \code{impact_fit}) receive
\strong{one shared draw} to preserve cross-analyte coherence — the same
perturbed fit object is assigned to every pooled analyte.  Non-pooled
\code{impact_fit}s and all \code{wq_fit}s are perturbed independently.
}
\keyword{internal}
