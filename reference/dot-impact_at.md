# Impact estimate `beta·f(hydro) + S_interp` at query dates (subset by `mask`)

Impact estimate `beta·f(hydro) + S_interp` at query dates (subset by
`mask`)

## Usage

``` r
.impact_at(m, feats, qdates, mask)
```
