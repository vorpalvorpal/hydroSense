# Deterministic season-blind hydrology prediction `beta.f(hydro)` at query dates

Deterministic season-blind hydrology prediction `beta.f(hydro)` at query
dates

## Usage

``` r
.hydro_pred(m, feats, qdates)
```
